node_modules/
build/
dist/
.wrangler/

# Generated by `npm run spec:sync` -- formatting these fights the generator and
# makes the weekly sync diff unreadable. `guides/` belongs here for the same
# reason: it is Productive's own documentation mirrored to Markdown, rewritten
# on every sync (`npm run spec:guides` refreshes it even when the spec 304s).
docs/api-spec/productive-openapi.yaml
docs/api-spec/resources/
docs/api-spec/impact-baseline.json
docs/api-spec/guides/

# Claude Code slash-command definitions -- local tooling rather than project
# source, and outside the CI check (`prettier --check src scripts tests`).
# Formatting them would reflow 24 files that nothing else in the repo consumes.
.claude/commands/
