# Productive.io API – Resource Index
# Read this first, then read resources/{slug}.yaml for details.
#
# 138 files, 390 paths, 648 operations
# Generated by scripts/sync-spec.ts -- do not edit by hand.

activities:
  file: activities.yaml
  operations:
    - GET /api/v2/activities
    - GET /api/v2/activities/{id}
  description: Activities in Productive track changes made to various objects across the platform. Whenever an object is created, updated, or deleted, a new activity is logged. This activity records the type of change, the specific data that was modified, as well as the actor who made the change and the exact timestamp when it occurred.
agent_configs:
  file: agent_configs.yaml
  operations:
    - DELETE /api/v2/agent_configs/{id}
    - GET /api/v2/agent_configs
    - GET /api/v2/agent_configs/{id}
    - PATCH /api/v2/agent_configs/{id}
    - POST /api/v2/agent_configs
  description: Endpoints for managing agent configurations — AI behavior instructions and Slack trigger settings for an agent.
agent_roles:
  file: agent_roles.yaml
  operations:
    - GET /api/v2/agent_roles/{id}
    - PATCH /api/v2/agent_roles/{id}
  description: Endpoints for managing agent roles — custom permission sets that define what actions an AI agent is allowed to perform within the organization.
agents:
  file: agents.yaml
  operations:
    - GET /api/v2/agents
    - GET /api/v2/agents/{id}
    - PATCH /api/v2/agents/{id}
    - PATCH /api/v2/agents/{id}/activate
    - PATCH /api/v2/agents/{id}/deactivate
    - POST /api/v2/agents
  description: Endpoints for managing AI agents — automated organization members with configurable behavior, permissions, and Slack integration.
approval_policy:
  file: approval_policy.yaml
  operations:
    - GET /api/v2/approval_policies
    - GET /api/v2/approval_policies/{id}
    - PATCH /api/v2/approval_policies/{id}
    - PATCH /api/v2/approval_policies/{id}/archive
    - PATCH /api/v2/approval_policies/{id}/restore
    - POST /api/v2/approval_policies
  description: Approval policies define who approves time entries, absences, or expenses. Configures the set of approvers and whether all approvers must approve (unanimous) or any single approver is sufficient.
approval_policy_assignment:
  file: approval_policy_assignment.yaml
  operations:
    - DELETE /api/v2/approval_policy_assignments/{id}
    - GET /api/v2/approval_policy_assignments
    - GET /api/v2/approval_policy_assignments/{id}
    - PATCH /api/v2/approval_policy_assignments/{id}
    - POST /api/v2/approval_policy_assignments
  description: '**Approval policy assignments** link an approval policy to a person, determining who approves their time entries, expenses, or absence requests. Each person can have different policies for different approval types.'
approval_status:
  file: approval_status.yaml
  operations:
    - GET /api/v2/approval_statuses
    - GET /api/v2/approval_statuses/{id}
    - PATCH /api/v2/approval_statuses/{id}/approve
    - PATCH /api/v2/approval_statuses/{id}/reject
  description: The `ApprovalStatus` entity represents a single approval requirement on a target (`Booking`, `TimeEntry`, or `Expense`). It is created automatically by an `ApprovalWorkflow` once a triggering event matches the workflow's approval policy, and it tracks whether a specific approver has approved or rejected the target.
approval_workflow:
  file: approval_workflow.yaml
  operations:
    - DELETE /api/v2/approval_workflows/{id}
    - GET /api/v2/approval_workflows/{id}
    - PATCH /api/v2/approval_workflows/{id}
    - POST /api/v2/approval_workflows
  description: The ApprovalWorkflow entity serves as the core component of the [ApprovalPolicy](https://developer-staging.productive.io/approval_policies.html#approval-policies) object. It defines and manages information about approvers and subscribers, including the explicit and dynamic approvers and subscribers.
artifacts:
  file: artifacts.yaml
  operations:
    - DELETE /api/v2/artifacts/{id}
    - GET /api/v2/artifacts
    - GET /api/v2/artifacts/{id}
    - GET /api/v2/public/artifacts/{uuid}
    - GET /api/v2/public/artifacts/{uuid}/attachments/{id}/auth
    - PATCH /api/v2/artifacts/{id}/publish
    - PATCH /api/v2/artifacts/{id}/unpublish
attachments:
  file: attachments.yaml
  operations:
    - DELETE /api/v2/attachments/{id}
    - GET /api/v2/attachments
    - GET /api/v2/attachments/{id}
    - PATCH /api/v2/attachments/{id}
    - POST /api/v2/attachments
  description: The attachment object represents files such as images, PDFs and documents that can be assigned to other objects. These include, but are not limited, to tasks, comments, expenses and purchase orders. With attachments, users can provide relevant information, resources and additional context for related objects.
automatic_invoicing_rules:
  file: automatic_invoicing_rules.yaml
  operations:
    - DELETE /api/v2/automatic_invoicing_rules/{id}
    - GET /api/v2/automatic_invoicing_rules
    - GET /api/v2/automatic_invoicing_rules/{id}
    - PATCH /api/v2/automatic_invoicing_rules/{id}
    - POST /api/v2/automatic_invoicing_rules
  description: '**Automatic invoicing rules** define conditions under which invoices are generated automatically from budget services — on time-based schedules, milestone completion, or budget consumption thresholds.'
bankaccounts:
  file: bankaccounts.yaml
  operations:
    - GET /api/v2/bank_accounts
    - GET /api/v2/bank_accounts/{id}
    - PATCH /api/v2/bank_accounts/{id}
    - PATCH /api/v2/bank_accounts/{id}/archive
    - PATCH /api/v2/bank_accounts/{id}/restore
    - POST /api/v2/bank_accounts
  description: '**Bank accounts** store payment destination details for subsidiaries, displayed on invoices and financial documents. They contain IBAN, SWIFT/BIC, bank name, and account number fields.'
bills:
  file: bills.yaml
  operations:
    - DELETE /api/v2/bills/{id}
    - GET /api/v2/bills/{id}
    - PATCH /api/v2/bills/{id}
    - POST /api/v2/bills
  description: Receipt records against a purchase order tracking vendor deliveries. Each bill captures the invoice number, date, due date, description, and amounts received. Bill items link PO expenses to received quantities.
boards:
  file: boards.yaml
  operations:
    - GET /api/v2/boards
    - GET /api/v2/boards/{id}
    - PATCH /api/v2/boards/{id}
    - PATCH /api/v2/boards/{id}/archive
    - PATCH /api/v2/boards/{id}/move
    - PATCH /api/v2/boards/{id}/reposition
    - PATCH /api/v2/boards/{id}/restore
    - POST /api/v2/boards
    - POST /api/v2/boards/copy
  description: The Board object represents a board within the project management system. It sits between Projects and Task lists, meaning it belongs to a project and contains multiple task lists.
bookings:
  file: bookings.yaml
  operations:
    - DELETE /api/v2/bookings/{id}
    - GET /api/v2/bookings
    - GET /api/v2/bookings/{id}
    - PATCH /api/v2/bookings/{id}
    - POST /api/v2/bookings
  description: 'The Booking object represents a scheduled time allocation for a person, serving as a core component for tracking time, managing availability, and organizing resource allocation. There are two main types of bookings:'
comments:
  file: comments.yaml
  operations:
    - DELETE /api/v2/comments/{id}
    - GET /api/v2/comments
    - GET /api/v2/comments/{id}
    - PATCH /api/v2/comments/{id}
    - PATCH /api/v2/comments/{id}/add_reaction
    - PATCH /api/v2/comments/{id}/pin
    - PATCH /api/v2/comments/{id}/remove_reaction
    - PATCH /api/v2/comments/{id}/unpin
    - POST /api/v2/comments
  description: Comments on tasks, deals, companies, persons, pages, projects, purchase orders, and invoices. Supports rich text with @mentions, emoji reactions, pinning, and edit history. Comments appear in the activity feed of the parent resource.
companies:
  file: companies.yaml
  operations:
    - GET /api/v2/companies
    - GET /api/v2/companies/{id}
    - PATCH /api/v2/companies/{id}
    - PATCH /api/v2/companies/{id}/archive
    - PATCH /api/v2/companies/{id}/restore
    - POST /api/v2/companies
  description: Company resources are used to keep track of clients and manage their associated deals, budgets, contacts (people) and projects.
contact_entries:
  file: contact_entries.yaml
  operations:
    - DELETE /api/v2/contact_entries/{id}
    - GET /api/v2/contact_entries
    - GET /api/v2/contact_entries/{id}
    - PATCH /api/v2/contact_entries/{id}
    - POST /api/v2/contact_entries
  description: Contact information records (email, phone, website, postal address) for companies and people. Multiple contact entries can be attached to a single company or person, each with a type (e.g. work, personal) and optional billing-specific fields.
contracts:
  file: contracts.yaml
  operations:
    - DELETE /api/v2/contracts/{id}
    - GET /api/v2/contracts
    - GET /api/v2/contracts/{id}
    - PATCH /api/v2/contracts/{id}
    - POST /api/v2/contracts
    - POST /api/v2/contracts/{id}/generate
  description: For more details on contracts please refer to the [help article](https://help.productive.io/en/articles/2179582-recurring-budgets).
custom_domains:
  file: custom_domains.yaml
  operations:
    - DELETE /api/v2/custom_domains/{id}
    - GET /api/v2/custom_domains
    - GET /api/v2/custom_domains/{id}
    - POST /api/v2/custom_domains
  description: '**Custom domains** allow organizations to send transactional emails (invoices, proposals, notifications) from their own domain rather than a Productive-managed address.'
custom_field_options:
  file: custom_field_options.yaml
  operations:
    - GET /api/v2/custom_field_options
    - GET /api/v2/custom_field_options/{id}
    - PATCH /api/v2/custom_field_options/{id}
    - PATCH /api/v2/custom_field_options/{id}/archive
    - POST /api/v2/custom_field_options
  description: Custom field options define the choices available for custom fields with the select or multiple select data types.
custom_field_section:
  file: custom_field_section.yaml
  operations:
    - GET /api/v2/custom_field_sections
    - GET /api/v2/custom_field_sections/{id}
    - PATCH /api/v2/custom_field_sections/{id}
    - PATCH /api/v2/custom_field_sections/{id}/archive
    - POST /api/v2/custom_field_sections
  description: CustomFieldSection is an entity that serves as a container for employee custom fields, enabling the grouping of those fields. Also in this way, employee custom fields cannot exist without belonging to a section.
custom_fields:
  file: custom_fields.yaml
  operations:
    - GET /api/v2/custom_fields
    - GET /api/v2/custom_fields/{id}
    - PATCH /api/v2/custom_fields/{id}
    - PATCH /api/v2/custom_fields/{id}/archive
    - POST /api/v2/custom_fields
  description: Custom fields allow you to use unique categorizations that are not available in Productive's default settings, allowing you to organize data based on your specific criteria and use it throughout the app.
dashboards:
  file: dashboards.yaml
  operations:
    - DELETE /api/v2/dashboards/{id}
    - GET /api/v2/dashboards
    - GET /api/v2/dashboards/{id}
    - PATCH /api/v2/dashboards/{id}
    - POST /api/v2/dashboards
    - POST /api/v2/dashboards/copy
  description: The Dashboard object represents the main component of the dashboard system.
deal_cost_rates:
  file: deal_cost_rates.yaml
  operations:
    - DELETE /api/v2/deal_cost_rates/{id}
    - GET /api/v2/deal_cost_rates
    - GET /api/v2/deal_cost_rates/{id}
    - PATCH /api/v2/deal_cost_rates/{id}
    - POST /api/v2/deal_cost_rates
  description: For an overview of deal cost rates please refer to the [help article](https://help.productive.io/en/articles/8795140-custom-cost-rates-on-budgets-and-deals).
deal_statuses:
  file: deal_statuses.yaml
  operations:
    - DELETE /api/v2/deal_statuses/{id}
    - GET /api/v2/deal_statuses
    - GET /api/v2/deal_statuses/{id}
    - PATCH /api/v2/deal_statuses/merge
    - PATCH /api/v2/deal_statuses/{id}
    - PATCH /api/v2/deal_statuses/{id}/archive
    - POST /api/v2/deal_statuses
  description: Pipeline stages defining deal outcomes. Each status belongs to a pipeline and has an outcome category (open, won, lost, or delivered). Statuses can enable time tracking, expense tracking, booking tracking, and probability estimation.
deals:
  file: deals.yaml
  operations:
    - DELETE /api/v2/deals/{id}
    - GET /api/v2/deals
    - GET /api/v2/deals/{id}
    - PATCH /api/v2/deals/{id}
    - PATCH /api/v2/deals/{id}/close
    - PATCH /api/v2/deals/{id}/open
    - POST /api/v2/deals
    - POST /api/v2/deals/copy
    - POST /api/v2/deals/create_from_origin
  description: This endpoint contains both `budgets` and `deals`.
deleted_items:
  file: deleted_items.yaml
  operations:
    - GET /api/v2/deleted_items
    - GET /api/v2/deleted_items/{id}
    - PATCH /api/v2/deleted_items/{id}/restore
  description: The Deleted item object represents an deleted object (e.g., Task, Project, Doc) that can be found in the Recycle bin.
discussions:
  file: discussions.yaml
  operations:
    - DELETE /api/v2/discussions/{id}
    - GET /api/v2/discussions
    - GET /api/v2/discussions/{id}
    - PATCH /api/v2/discussions/{id}
    - PATCH /api/v2/discussions/{id}/reopen
    - PATCH /api/v2/discussions/{id}/resolve
    - PATCH /api/v2/discussions/{id}/subscribe
    - PATCH /api/v2/discussions/{id}/unsubscribe
    - POST /api/v2/discussions
  description: Discussion is a thread of comments related to the highlighted part of docs (page) content.
document_styles:
  file: document_styles.yaml
  operations:
    - DELETE /api/v2/document_styles/{id}
    - GET /api/v2/document_styles
    - GET /api/v2/document_styles/{id}
    - PATCH /api/v2/document_styles/{id}
    - POST /api/v2/document_styles
    - POST /api/v2/document_styles/copy
  description: '**Document styles** control the visual presentation of generated PDFs — fonts, colors, logos, and layout settings. They are applied to document types to produce consistently branded, client-facing documents.'
document_types:
  file: document_types.yaml
  operations:
    - GET /api/v2/document_types
    - GET /api/v2/document_types/{id}
    - PATCH /api/v2/document_types/{id}
    - PATCH /api/v2/document_types/{id}/archive
    - POST /api/v2/document_types
    - POST /api/v2/document_types/copy
  description: '**Document types** are PDF templates used to generate financial documents — invoices, proposals, and purchase orders. They define content blocks, layout, and branding applied when rendering documents.'
e_invoice_identities:
  file: e_invoice_identities.yaml
  operations:
    - GET /api/v2/einvoice_identities
    - GET /api/v2/einvoice_identities/{id}
    - PATCH /api/v2/einvoice_identities/{id}
    - POST /api/v2/einvoice_identities
emails:
  file: emails.yaml
  operations:
    - DELETE /api/v2/emails/{id}
    - GET /api/v2/emails
    - GET /api/v2/emails/{id}
    - PATCH /api/v2/emails/{id}/attach
    - PATCH /api/v2/emails/{id}/dismiss
  description: '**Emails** synced from external providers (Gmail, Outlook) and linked to deals, contacts, projects, or tasks. They surface client communication history directly within Productive for CRM context.'
entitlements:
  file: entitlements.yaml
  operations:
    - DELETE /api/v2/entitlements/{id}
    - GET /api/v2/entitlements
    - GET /api/v2/entitlements/{id}
    - PATCH /api/v2/entitlements/{id}
    - POST /api/v2/entitlements
  description: Time-off balances for a person. Tracks allocated, used, and pending (approved but not taken) days for a specific absence category (event) and period. Updated automatically as bookings and approvals are processed.
events:
  file: events.yaml
  operations:
    - DELETE /api/v2/events/{id}
    - GET /api/v2/events
    - GET /api/v2/events/{id}
    - PATCH /api/v2/events/{id}
    - PATCH /api/v2/events/{id}/archive
    - POST /api/v2/events
  description: 'Event resources represent Absence categories. More information about the Absence categories can be found in our Help documentation: [Configuring Your Time Off Settings](https://help.productive.io/en/articles/3154360-configuring-your-time-off-settings).'
exchange_rates:
  file: exchange_rates.yaml
  operations:
    - GET /api/v2/exchange_rates
  description: '**Exchange rates** define currency conversion ratios applied across financial calculations in Productive — used when converting budget amounts, costs, and revenues between currencies in multi-currency organizations.'
expenses:
  file: expenses.yaml
  operations:
    - DELETE /api/v2/expenses/{id}
    - GET /api/v2/expenses
    - GET /api/v2/expenses/{id}
    - PATCH /api/v2/expenses/{id}
    - PATCH /api/v2/expenses/{id}/approve
    - PATCH /api/v2/expenses/{id}/export
    - PATCH /api/v2/expenses/{id}/export_update
    - PATCH /api/v2/expenses/{id}/reject
    - PATCH /api/v2/expenses/{id}/unapprove
    - PATCH /api/v2/expenses/{id}/unreject
    - POST /api/v2/expenses
    - POST /api/v2/expenses/copy
  description: For an overview of expenses please refer to the [help article](https://help.productive.io/en/articles/9154726-expenses-admin-overview)
expenses_bulk:
  file: expenses_bulk.yaml
  operations:
    - DELETE /api/v2/expenses
    - PATCH /api/v2/expenses
    - PATCH /api/v2/expenses/bulk_approve
  description: '**Bulk expense operations** allow creating or updating multiple expense records in a single request — useful for high-volume import workflows where individual API calls would be impractical.'
filters:
  file: filters.yaml
  operations:
    - DELETE /api/v2/filters/{id}
    - GET /api/v2/filters
    - GET /api/v2/filters/{id}
    - PATCH /api/v2/filters/{id}
    - POST /api/v2/filters
  description: Filters records are used to save the setup of your views, reports and widgets across Productive.
folders:
  file: folders.yaml
  operations:
    - GET /api/v2/folders
    - GET /api/v2/folders/{id}
    - PATCH /api/v2/folders/{id}
    - PATCH /api/v2/folders/{id}/archive
    - PATCH /api/v2/folders/{id}/move
    - PATCH /api/v2/folders/{id}/reposition
    - PATCH /api/v2/folders/{id}/restore
    - POST /api/v2/folders
    - POST /api/v2/folders/copy
  description: The Folder object represents a folder within the project management system. It sits between Projects and Task lists, meaning it belongs to a project and contains multiple task lists.
holiday_calendars:
  file: holiday_calendars.yaml
  operations:
    - DELETE /api/v2/holiday_calendars/{id}
    - GET /api/v2/holiday_calendars
    - GET /api/v2/holiday_calendars/{id}
    - PATCH /api/v2/holiday_calendars/{id}
    - POST /api/v2/holiday_calendars
  description: For more details on how to work with holiday calendars and holidays, please refer to [Holidays Settings](https://help.productive.io/en/articles/2179650-holidays-settings) article.
holidays:
  file: holidays.yaml
  operations:
    - DELETE /api/v2/holidays/{id}
    - GET /api/v2/holidays
    - GET /api/v2/holidays/{id}
    - PATCH /api/v2/holidays/{id}
    - POST /api/v2/holidays
  description: 'For more details on how to work with holidays please refer to: [Holidays Settings](https://help.productive.io/en/articles/2179650-holidays-settings).'
integration_exporter_configuration:
  file: integration_exporter_configuration.yaml
  operations:
    - DELETE /api/v2/integration_exporter_configurations/{id}
    - GET /api/v2/integration_exporter_configurations
    - GET /api/v2/integration_exporter_configurations/{id}
    - PATCH /api/v2/integration_exporter_configurations/{id}
    - POST /api/v2/integration_exporter_configurations
  description: '**Integration exporter configurations** define how data is exported from Productive to external accounting and ERP systems, storing connection credentials, entity mappings, and export preferences per integration type.'
integration_task_management_configuration:
  file: integration_task_management_configuration.yaml
  operations:
    - DELETE /api/v2/integration_task_management_configurations/{id}
    - GET /api/v2/integration_task_management_configurations
    - GET /api/v2/integration_task_management_configurations/{id}
    - PATCH /api/v2/integration_task_management_configurations/{id}
    - POST /api/v2/integration_task_management_configurations
integrations:
  file: integrations.yaml
  operations:
    - DELETE /api/v2/integrations/{id}
    - GET /api/v2/integrations
    - GET /api/v2/integrations/{id}
    - GET /api/v2/integrations/{id}/check
    - PATCH /api/v2/integrations/{id}/connect
    - POST /api/v2/integrations
  description: '**Integrations** connect Productive to external systems — accounting software, SSO providers, and communication tools. Manage settings, credentials, and sync configuration for each connected service.'
invitations:
  file: invitations.yaml
  operations:
    - GET /api/v2/invitations/{id}
    - PATCH /api/v2/invitations/{id}
    - POST /api/v2/invitations
  description: '**Invitations** allow existing users to invite new members to join an organization in Productive.'
invoice_attributions:
  file: invoice_attributions.yaml
  operations:
    - DELETE /api/v2/invoice_attributions/{id}
    - GET /api/v2/invoice_attributions
    - GET /api/v2/invoice_attributions/{id}
    - PATCH /api/v2/invoice_attributions/{id}
    - POST /api/v2/invoice_attributions
  description: An invoice attribution object represents a link between a budget and an invoice.
invoice_templates:
  file: invoice_templates.yaml
  operations:
    - DELETE /api/v2/invoice_templates/{id}
    - GET /api/v2/invoice_templates
    - GET /api/v2/invoice_templates/{id}
    - PATCH /api/v2/invoice_templates/{id}
    - POST /api/v2/invoice_templates
  description: '**Invoice templates** (document types) define the layout, content, and branding for PDF-generated invoices and financial documents — controlling headers, footers, logos, and displayed fields.'
invoices:
  file: invoices.yaml
  operations:
    - DELETE /api/v2/invoices/{id}
    - GET /api/v2/invoices
    - GET /api/v2/invoices/{id}
    - GET /api/v2/invoices/{id}/preview
    - PATCH /api/v2/invoices/{id}
    - PATCH /api/v2/invoices/{id}/export
    - PATCH /api/v2/invoices/{id}/export_update
    - PATCH /api/v2/invoices/{id}/finalize
    - PATCH /api/v2/invoices/{id}/send
    - PATCH /api/v2/invoices/{id}/send_einvoice
    - POST /api/v2/invoices
  description: For more details on how to work with custom fields please refer to [Working with custom fields](https://developer.productive.io/working_with_custom_fields.html#working_with_custom_fields) guide.
job_role:
  file: job_role.yaml
  operations:
    - GET /api/v2/job_roles
    - GET /api/v2/job_roles/{id}
    - PATCH /api/v2/job_roles/{id}
    - PATCH /api/v2/job_roles/{id}/archive
    - PATCH /api/v2/job_roles/{id}/restore
    - POST /api/v2/job_roles
line_items:
  file: line_items.yaml
  operations:
    - DELETE /api/v2/line_items/{id}
    - GET /api/v2/line_items
    - GET /api/v2/line_items/{id}
    - PATCH /api/v2/line_items/{id}
    - POST /api/v2/line_items/generate
  description: A line item is a component of an invoice which represents an individual entry that details a specific good or service provided by the seller to the buyer.
line_items_bulk:
  file: line_items_bulk.yaml
  operations:
    - DELETE /api/v2/line_items
    - PATCH /api/v2/line_items
    - POST /api/v2/line_items
lost_reasons:
  file: lost_reasons.yaml
  operations:
    - GET /api/v2/lost_reasons
    - GET /api/v2/lost_reasons/{id}
    - PATCH /api/v2/lost_reasons/{id}
    - PATCH /api/v2/lost_reasons/{id}/archive
    - POST /api/v2/lost_reasons
  description: '**Lost reasons** are labels that explain why a deal was marked as lost in the CRM pipeline.'
memberships:
  file: memberships.yaml
  operations:
    - DELETE /api/v2/memberships/{id}
    - GET /api/v2/memberships
    - GET /api/v2/memberships/{id}
    - PATCH /api/v2/memberships/{id}
    - POST /api/v2/memberships
  description: 'Working with memberships:'
notifications:
  file: notifications.yaml
  operations:
    - GET /api/v2/notifications
    - GET /api/v2/notifications/{id}
    - PATCH /api/v2/notifications/{id}/dismiss
    - PATCH /api/v2/notifications/{id}/read
    - PATCH /api/v2/notifications/{id}/undismiss
    - PATCH /api/v2/notifications/{id}/unread
  description: '**Notifications** are inbox items generated by activity on tasks, deals, comments, and other resources — triggered by mentions, assignments, approval requests, and status changes.'
organization_memberships:
  file: organization_memberships.yaml
  operations:
    - DELETE /api/v2/organization_memberships/{id}
    - GET /api/v2/organization_memberships
    - GET /api/v2/organization_memberships/{id}
    - PATCH /api/v2/organization_memberships/{id}
    - PATCH /api/v2/organization_memberships/{id}/clear_notifications
    - PATCH /api/v2/organization_memberships/{id}/dismiss_notifications
    - PATCH /api/v2/organization_memberships/{id}/read_notifications
  description: Organization memberships link users to organizations, granting login access. Stores per-user preferences including notification settings, default filters for each list view (tasks, deals, invoices, etc.), and UI layout preferences.
organization_subscriptions:
  file: organization_subscriptions.yaml
  operations:
    - GET /api/v2/organization_subscriptions/{id}
    - PATCH /api/v2/organization_subscriptions/{id}
    - PATCH /api/v2/organization_subscriptions/{id}/cancel
    - PATCH /api/v2/organization_subscriptions/{id}/reactivate
    - PATCH /api/v2/organization_subscriptions/{id}/retry_last_payment
  description: '**Organization subscriptions** represent the current pricing plan and active feature entitlements for an organization.'
organizations:
  file: organizations.yaml
  operations:
    - DELETE /api/v2/organizations/{id}
    - GET /api/v2/organizations
    - GET /api/v2/organizations/{id}
    - PATCH /api/v2/organizations/{id}
    - PATCH /api/v2/organizations/{id}/resend_code
  description: '**Organizations** are the top-level accounts representing a company using Productive. Manage organization-wide settings, branding, localization, and configuration.'
overheads:
  file: overheads.yaml
  operations:
    - GET /api/v2/overheads
    - GET /api/v2/overheads/{id}
    - PATCH /api/v2/overheads/{id}
    - PATCH /api/v2/overheads/{id}/recalculate
  description: For more details about what overheads are, please refer to the [help article](https://help.productive.io/en/articles/6839043-overhead-cost).
pages:
  file: pages.yaml
  operations:
    - DELETE /api/v2/pages/{id}
    - GET /api/v2/pages
    - GET /api/v2/pages/{id}
    - PATCH /api/v2/pages/{id}/append_html
    - PATCH /api/v2/pages/{id}/append_markdown
    - PATCH /api/v2/pages/{id}/move
    - PATCH /api/v2/pages/{id}/publish
    - PATCH /api/v2/pages/{id}/replace_body_with_html
    - PATCH /api/v2/pages/{id}/replace_body_with_markdown
    - PATCH /api/v2/pages/{id}/unpublish
    - POST /api/v2/pages/copy
    - POST /api/v2/pages/create_with_markdown
  description: Rich text pages organized in a Doc hierarchy. Root pages (Docs) support public sharing and permission management. Child pages inherit from their parent Doc. Pages support custom fields, edit history, and inline discussions.
pageversions:
  file: pageversions.yaml
  operations:
    - GET /api/v2/page_versions
    - GET /api/v2/page_versions/{id}
    - PATCH /api/v2/page_versions/{id}
    - POST /api/v2/page_versions
  description: '**Page versions** capture the revision history of a doc page — each significant edit creates a new version.'
passwords:
  file: passwords.yaml
  operations:
    - GET /api/v2/passwords/{id}
    - PATCH /api/v2/passwords/{id}
    - POST /api/v2/passwords
  description: Manage **user password changes and resets**.
payment_reminder_sequences:
  file: payment_reminder_sequences.yaml
  operations:
    - DELETE /api/v2/payment_reminder_sequences/{id}
    - GET /api/v2/payment_reminder_sequences
    - GET /api/v2/payment_reminder_sequences/{id}
    - PATCH /api/v2/payment_reminder_sequences/{id}
    - POST /api/v2/payment_reminder_sequences
  description: The Payment reminder sequence object represents payment reminder templates.
payments:
  file: payments.yaml
  operations:
    - DELETE /api/v2/payments/{id}
    - GET /api/v2/payments
    - GET /api/v2/payments/{id}
    - PATCH /api/v2/payments/{id}
    - POST /api/v2/payments
  description: The Payment object represents an invoice payment in invoicing system.
people:
  file: people.yaml
  operations:
    - GET /api/v2/people
    - GET /api/v2/people/{id}
    - PATCH /api/v2/people/merge
    - PATCH /api/v2/people/{id}
    - PATCH /api/v2/people/{id}/activate
    - PATCH /api/v2/people/{id}/archive
    - PATCH /api/v2/people/{id}/deactivate
    - PATCH /api/v2/people/{id}/invite
    - PATCH /api/v2/people/{id}/regenerate_recovery_codes
    - PATCH /api/v2/people/{id}/resend
    - PATCH /api/v2/people/{id}/restore
    - PATCH /api/v2/people/{id}/virtualize
    - POST /api/v2/people
  description: For more details on how to work with custom fields please refer to [Working with custom fields](https://developer.productive.io/working_with_custom_fields.html#working_with_custom_fields) guide.
permission_sets:
  file: permission_sets.yaml
  operations:
    - DELETE /api/v2/roles/{id}
    - GET /api/v2/roles
    - GET /api/v2/roles/{id}
    - PATCH /api/v2/roles/{id}
    - POST /api/v2/roles
  description: The Permission Set object represents a set of permissions that can be assigned to a user.
pipelines:
  file: pipelines.yaml
  operations:
    - DELETE /api/v2/pipelines/{id}
    - GET /api/v2/pipelines
    - GET /api/v2/pipelines/{id}
    - PATCH /api/v2/pipelines/{id}
    - POST /api/v2/pipelines
  description: For more details about pipelines and how to use them with deal statuses, please refer to the [help article](https://help.productive.io/en/articles/2179572-setting-up-your-sales-pipelines).
placeholder_usages:
  file: placeholder_usages.yaml
  operations:
    - DELETE /api/v2/placeholder_usages/{id}
    - GET /api/v2/placeholder_usages
    - GET /api/v2/placeholder_usages/{id}
    - PATCH /api/v2/placeholder_usages/{id}
    - POST /api/v2/placeholder_usages
  description: '**Placeholder usages** track how a placeholder''s allocated capacity is distributed across projects and time periods.'
placeholders:
  file: placeholders.yaml
  operations:
    - DELETE /api/v2/placeholders/{id}
    - GET /api/v2/placeholders
    - GET /api/v2/placeholders/{id}
    - PATCH /api/v2/placeholders/{id}
    - POST /api/v2/placeholders
  description: '**Placeholders** are unnamed resource slots used in capacity planning before a specific person is assigned. They represent a role, skill, or headcount requirement on a project — allowing teams to schedule capacity without committing to a named individual.'
prices:
  file: prices.yaml
  operations:
    - DELETE /api/v2/prices/{id}
    - GET /api/v2/prices
    - GET /api/v2/prices/{id}
    - PATCH /api/v2/prices/{id}
    - POST /api/v2/prices
  description: Rate card line items with billing configuration. Each price belongs to a rate card and defines a service type, billing type (hourly, fixed, etc.), unit, rate, and optional tracking defaults. Supports discounts, markups, budget caps, and custom fields.
project_assignments:
  file: project_assignments.yaml
  operations:
    - DELETE /api/v2/project_assignments/{id}
    - GET /api/v2/project_assignments
    - GET /api/v2/project_assignments/{id}
    - PATCH /api/v2/project_assignments/{id}
    - POST /api/v2/project_assignments
  description: '**Project assignments** link people to projects, granting access and [Memberships](openapi:group:memberships). They determine which projects appear in a person''s task lists and time tracking views.'
projects:
  file: projects.yaml
  operations:
    - DELETE /api/v2/projects/{id}
    - GET /api/v2/projects
    - GET /api/v2/projects/{id}
    - PATCH /api/v2/projects/{id}
    - PATCH /api/v2/projects/{id}/apply_navigation_tabs
    - PATCH /api/v2/projects/{id}/archive
    - PATCH /api/v2/projects/{id}/change_workflow
    - PATCH /api/v2/projects/{id}/restore
    - PATCH /api/v2/projects/{id}/subscribe
    - PATCH /api/v2/projects/{id}/unsubscribe
    - POST /api/v2/projects
    - POST /api/v2/projects/copy
  description: For more details on how to work with custom fields please refer to [Working with custom fields](https://developer.productive.io/working_with_custom_fields.html#working_with_custom_fields) guide.
proposals:
  file: proposals.yaml
  operations:
    - DELETE /api/v2/proposals/{id}
    - GET /api/v2/proposals
    - GET /api/v2/proposals/{id}
    - GET /api/v2/proposals/{id}/sync_status
    - PATCH /api/v2/proposals/{id}
    - PATCH /api/v2/proposals/{id}/sync
    - PATCH /api/v2/public/proposals/{uuid}/accept
    - POST /api/v2/proposals
  description: '**Proposals** are sales quotes attached to deals, presenting scope and pricing for client review and e-signature. A proposal mirrors the deal''s sections and services in a client-facing layout with a pricing breakdown and acceptance workflow.'
pulses:
  file: pulses.yaml
  operations:
    - DELETE /api/v2/pulses/{id}
    - GET /api/v2/pulses
    - GET /api/v2/pulses/{id}
    - PATCH /api/v2/pulses/{id}
    - PATCH /api/v2/pulses/{id}/send
    - POST /api/v2/pulses
  description: A Pulse is an automated report delivery service that makes it easy to stay updated without having to manually check your reports. You can set up Pulses for any of your reports, choosing how often you’d like to receive them — whether it's daily, weekly, bi-weekly, or monthly. This way, important data arrives straight to your inbox or Slack, keeping you informed at your preferred schedule.
purchase_orders:
  file: purchase_orders.yaml
  operations:
    - DELETE /api/v2/purchase_orders/{id}
    - GET /api/v2/purchase_orders
    - GET /api/v2/purchase_orders/{id}
    - PATCH /api/v2/purchase_orders/{id}
    - PATCH /api/v2/purchase_orders/{id}/export
    - PATCH /api/v2/purchase_orders/{id}/export_update
    - PATCH /api/v2/purchase_orders/{id}/send
    - POST /api/v2/purchase_orders
    - POST /api/v2/purchase_orders/copy
  description: In Productive purchase orders are documents that serve as a buyer's request to a seller to order goods or services.
purchase_orders_bulk:
  file: purchase_orders_bulk.yaml
  operations:
    - DELETE /api/v2/purchase_orders
    - PATCH /api/v2/purchase_orders
  description: Bulk update and delete operations on purchase orders.
rate_cards:
  file: rate_cards.yaml
  operations:
    - DELETE /api/v2/rate_cards/{id}
    - GET /api/v2/rate_cards
    - GET /api/v2/rate_cards/{id}
    - PATCH /api/v2/rate_cards/{id}
    - PATCH /api/v2/rate_cards/{id}/archive
    - PATCH /api/v2/rate_cards/{id}/restore
    - POST /api/v2/rate_cards
    - POST /api/v2/rate_cards/copy
  description: Price lists for services. Rate cards define company-wide or client-specific pricing templates. Each rate card contains prices for service types with billing configuration. Client-specific rate cards override defaults for that company.
report_category:
  file: report_category.yaml
  operations:
    - DELETE /api/v2/report_categories/{id}
    - GET /api/v2/report_categories
    - GET /api/v2/report_categories/{id}
    - PATCH /api/v2/report_categories/{id}
    - POST /api/v2/report_categories
  description: Report categories help you organize and categorize your reports. You can assign each category a unique name and color code, making it easier to navigate through and manage your saved reports. This visual organization allows you to quickly find the reports you need and keep your workspace more streamlined.
reports/booking_reports:
  file: reports/booking_reports.yaml
  operations:
    - GET /api/v2/reports/booking_reports
reports/budget_reports:
  file: reports/budget_reports.yaml
  operations:
    - GET /api/v2/reports/budget_reports
reports/company_reports:
  file: reports/company_reports.yaml
  operations:
    - GET /api/v2/reports/company_reports
reports/deal_funnel_reports:
  file: reports/deal_funnel_reports.yaml
  operations:
    - GET /api/v2/reports/deal_funnel_reports
reports/deal_reports:
  file: reports/deal_reports.yaml
  operations:
    - GET /api/v2/reports/deal_reports
reports/entitlement_reports:
  file: reports/entitlement_reports.yaml
  operations:
    - GET /api/v2/reports/entitlement_reports
reports/expense_reports:
  file: reports/expense_reports.yaml
  operations:
    - GET /api/v2/reports/expense_reports
reports/financial_item_reports:
  file: reports/financial_item_reports.yaml
  operations:
    - GET /api/v2/reports/financial_item_reports
reports/invoice_reports:
  file: reports/invoice_reports.yaml
  operations:
    - GET /api/v2/reports/invoice_reports
reports/line_item_reports:
  file: reports/line_item_reports.yaml
  operations:
    - GET /api/v2/reports/line_item_reports
reports/page_reports:
  file: reports/page_reports.yaml
  operations:
    - GET /api/v2/reports/page_reports
reports/payment_reports:
  file: reports/payment_reports.yaml
  operations:
    - GET /api/v2/reports/payment_reports
reports/payroll_item_reports:
  file: reports/payroll_item_reports.yaml
  operations:
    - GET /api/v2/reports/payroll_item_reports
reports/person_reports:
  file: reports/person_reports.yaml
  operations:
    - GET /api/v2/reports/person_reports
reports/price_reports:
  file: reports/price_reports.yaml
  operations:
    - GET /api/v2/reports/price_reports
reports/project_reports:
  file: reports/project_reports.yaml
  operations:
    - GET /api/v2/reports/project_reports
reports/proposal_reports:
  file: reports/proposal_reports.yaml
  operations:
    - GET /api/v2/reports/proposal_reports
reports/resource_request_reports:
  file: reports/resource_request_reports.yaml
  operations:
    - GET /api/v2/reports/resource_request_reports
reports/salary_reports:
  file: reports/salary_reports.yaml
  operations:
    - GET /api/v2/reports/salary_reports
reports/service_reports:
  file: reports/service_reports.yaml
  operations:
    - GET /api/v2/reports/service_reports
reports/survey_reports:
  file: reports/survey_reports.yaml
  operations:
    - GET /api/v2/reports/survey_reports
reports/task_reports:
  file: reports/task_reports.yaml
  operations:
    - GET /api/v2/reports/task_reports
reports/time_entry_reports:
  file: reports/time_entry_reports.yaml
  operations:
    - GET /api/v2/reports/time_entry_reports
reports/time_reports:
  file: reports/time_reports.yaml
  operations:
    - GET /api/v2/reports/time_reports
reports/timesheet_reports:
  file: reports/timesheet_reports.yaml
  operations:
    - GET /api/v2/reports/timesheet_reports
resource_requests:
  file: resource_requests.yaml
  operations:
    - DELETE /api/v2/resource_requests/{id}
    - GET /api/v2/resource_requests
    - GET /api/v2/resource_requests/{id}
    - PATCH /api/v2/resource_requests/{id}
    - PATCH /api/v2/resource_requests/{id}/cancel
    - PATCH /api/v2/resource_requests/{id}/reject
    - POST /api/v2/resource_requests
    - POST /api/v2/resource_requests/{id}/resolve
  description: Operations for managing resource requests — requests to allocate or hire resources for a project service.
revenue_distributions:
  file: revenue_distributions.yaml
  operations:
    - DELETE /api/v2/revenue_distributions/{id}
    - GET /api/v2/revenue_distributions
    - GET /api/v2/revenue_distributions/{id}
    - PATCH /api/v2/revenue_distributions/{id}
    - POST /api/v2/revenue_distributions
  description: '**Revenue distributions** define how recognized revenue is allocated across time periods for a service, supporting accrual-based revenue recognition.'
salaries:
  file: salaries.yaml
  operations:
    - DELETE /api/v2/salaries/{id}
    - GET /api/v2/salaries
    - GET /api/v2/salaries/{id}
    - PATCH /api/v2/salaries/{id}
    - POST /api/v2/salaries
  description: For more details on salaries (also called cost rates) please refer to the [help article](https://help.productive.io/en/articles/2179644-understanding-and-setting-up-cost-rates-in-productive).
sections:
  file: sections.yaml
  operations:
    - DELETE /api/v2/sections/{id}
    - GET /api/v2/sections
    - GET /api/v2/sections/{id}
    - PATCH /api/v2/sections/{id}
    - POST /api/v2/sections
  description: '**Sections** group services within a deal or budget — typically organized by phase, deliverable type, or billing period. They provide structure for complex budgets and are reflected in proposals, invoices, and financial reporting.'
service_assignments:
  file: service_assignments.yaml
  operations:
    - DELETE /api/v2/service_assignments/{id}
    - GET /api/v2/service_assignments
    - GET /api/v2/service_assignments/{id}
    - PATCH /api/v2/service_assignments/{id}
    - POST /api/v2/service_assignments
  description: To restrict time tracking & expense logging, there is an option to assign specific people to specific services.
service_type_assignments:
  file: service_type_assignments.yaml
  operations:
    - DELETE /api/v2/service_type_assignments/{id}
    - GET /api/v2/service_type_assignments
    - GET /api/v2/service_type_assignments/{id}
    - PATCH /api/v2/service_type_assignments/{id}
    - POST /api/v2/service_type_assignments
  description: Service type assignments are tightly coupled with service types.
service_types:
  file: service_types.yaml
  operations:
    - GET /api/v2/service_types
    - GET /api/v2/service_types/{id}
    - PATCH /api/v2/service_types/merge
    - PATCH /api/v2/service_types/{id}
    - PATCH /api/v2/service_types/{id}/archive
    - POST /api/v2/service_types
  description: For more details about what service types are, please refer to the [help article](https://help.productive.io/en/articles/2179629-service-types-building-blocks-of-your-budget).
services:
  file: services.yaml
  operations:
    - DELETE /api/v2/services/{id}
    - GET /api/v2/services
    - GET /api/v2/services/{id}
    - PATCH /api/v2/services/{id}
    - POST /api/v2/services
  description: '**Services** are line items on a deal or budget that define the scope, pricing, and tracking for a deliverable. Each service specifies a billing type (`fixed_fee`, `hourly`, or `days`), a budget amount, and links time tracking to invoicing.'
sessions:
  file: sessions.yaml
  operations:
    - DELETE /api/v2/sessions/{id}
    - GET /api/v2/sessions
    - POST /api/v2/sessions
    - POST /api/v2/sessions/machine
    - PUT /api/v2/sessions/{id}/validate_otp
  description: '**Sessions** represent authenticated login sessions for users accessing Productive.'
subsidiaries:
  file: subsidiaries.yaml
  operations:
    - GET /api/v2/subsidiaries
    - GET /api/v2/subsidiaries/{id}
    - PATCH /api/v2/subsidiaries/{id}
    - PATCH /api/v2/subsidiaries/{id}/archive
    - POST /api/v2/subsidiaries
  description: '### Subsidiary Tax ID'
survey_field_options:
  file: survey_field_options.yaml
  operations:
    - GET /api/v2/survey_field_options
    - GET /api/v2/survey_field_options/{id}
    - PATCH /api/v2/survey_field_options/{id}
    - PATCH /api/v2/survey_field_options/{id}/archive
    - POST /api/v2/survey_field_options
  description: Selectable options for dropdown and multi-select survey fields. Each option has a label and position for ordering in the dropdown.
survey_fields:
  file: survey_fields.yaml
  operations:
    - GET /api/v2/survey_fields
    - GET /api/v2/survey_fields/{id}
    - PATCH /api/v2/survey_fields/{id}
    - PATCH /api/v2/survey_fields/{id}/archive
    - POST /api/v2/survey_fields
  description: Form fields on a survey. Each field has a data type (text, number, date, dropdown, checkbox, file) and optional configuration. Dropdown and multi-select fields have selectable options managed via survey_field_options.
survey_responses:
  file: survey_responses.yaml
  operations:
    - DELETE /api/v2/survey_responses/{id}
    - GET /api/v2/survey_responses
    - GET /api/v2/survey_responses/{id}
    - POST /api/v2/survey_responses
  description: Submitted survey responses. Each response links to a survey and contains field values for each survey field. Responses can trigger automations on submission.
surveys:
  file: surveys.yaml
  operations:
    - DELETE /api/v2/surveys/{id}
    - GET /api/v2/surveys
    - GET /api/v2/surveys/{id}
    - PATCH /api/v2/surveys/{id}
    - POST /api/v2/surveys
    - POST /api/v2/surveys/copy
  description: Project forms for structured data collection. Surveys can trigger automations on submission, making them useful for intake forms, onboarding workflows, and status updates. Supports custom fields for form fields.
tags:
  file: tags.yaml
  operations:
    - GET /api/v2/tags
    - GET /api/v2/tags/{id}
  description: '**Tags** are labels applied to tasks and other resources for categorization and filtering.'
task_lists:
  file: task_lists.yaml
  operations:
    - GET /api/v2/task_lists
    - GET /api/v2/task_lists/{id}
    - PATCH /api/v2/task_lists/{id}
    - PATCH /api/v2/task_lists/{id}/archive
    - PATCH /api/v2/task_lists/{id}/move
    - PATCH /api/v2/task_lists/{id}/reposition
    - PATCH /api/v2/task_lists/{id}/restore
    - POST /api/v2/task_lists
    - POST /api/v2/task_lists/copy
  description: The Task list object represents a task list within the project management system. It sits between Folders and Tasks, meaning it belongs to a folder and contains multiple tasks.
taskdependency:
  file: taskdependency.yaml
  operations:
    - DELETE /api/v2/task_dependencies/{id}
    - GET /api/v2/task_dependencies
    - GET /api/v2/task_dependencies/{id}
    - PATCH /api/v2/task_dependencies/{id}
    - POST /api/v2/task_dependencies
  description: '**Task dependencies** define ordering relationships between tasks: blocking, waiting on, or linked.'
tasks:
  file: tasks.yaml
  operations:
    - DELETE /api/v2/tasks/{id}
    - GET /api/v2/tasks
    - GET /api/v2/tasks/{id}
    - PATCH /api/v2/tasks/{id}
    - PATCH /api/v2/tasks/{id}/move_dependent
    - PATCH /api/v2/tasks/{id}/reposition
    - POST /api/v2/tasks
    - POST /api/v2/tasks/copy
  description: For more details on how to work with custom fields please refer to [Working with custom fields](https://developer.productive.io/working_with_custom_fields.html#working_with_custom_fields) guide.
taxrates:
  file: taxrates.yaml
  operations:
    - GET /api/v2/tax_rates
    - GET /api/v2/tax_rates/{id}
    - PATCH /api/v2/tax_rates/{id}
    - PATCH /api/v2/tax_rates/{id}/archive
    - PATCH /api/v2/tax_rates/{id}/restore
    - POST /api/v2/tax_rates
  description: The Tax rate object represents tax data in the invoicing system.
team_memberships:
  file: team_memberships.yaml
  operations:
    - DELETE /api/v2/team_memberships/{id}
    - GET /api/v2/team_memberships
    - GET /api/v2/team_memberships/{id}
    - POST /api/v2/team_memberships
  description: Person-team links. Each membership adds a person to a team. Teams are used for access control on projects and other resources, and for reporting groupings.
teams:
  file: teams.yaml
  operations:
    - DELETE /api/v2/teams/{id}
    - GET /api/v2/teams
    - GET /api/v2/teams/{id}
    - PATCH /api/v2/teams/{id}
    - POST /api/v2/teams
  description: Named groups of people used for reporting and access control. Teams can be granted access to projects and other resources via memberships. Each team has a list of member people.
templates:
  file: templates.yaml
  operations:
    - DELETE /api/v2/templates/{id}
    - GET /api/v2/templates
    - GET /api/v2/templates/{id}
    - PATCH /api/v2/templates/{id}
    - POST /api/v2/templates
  description: Reusable starting points for projects, budgets, deals, and pages. Each template wraps a real entity (its `target`) created with template semantics; the `target_type` discriminator determines the per-target shape required when creating a new template.
time_entries:
  file: time_entries.yaml
  operations:
    - DELETE /api/v2/time_entries/{id}
    - GET /api/v2/time_entries
    - GET /api/v2/time_entries/{id}
    - PATCH /api/v2/time_entries/{id}
    - PATCH /api/v2/time_entries/{id}/approve
    - PATCH /api/v2/time_entries/{id}/reject
    - PATCH /api/v2/time_entries/{id}/unapprove
    - PATCH /api/v2/time_entries/{id}/unreject
  description: Time entry object represents spent time for a person.
time_entries_bulk:
  file: time_entries_bulk.yaml
  operations:
    - DELETE /api/v2/time_entries
    - PATCH /api/v2/time_entries
    - PATCH /api/v2/time_entries/approve
    - PATCH /api/v2/time_entries/unapprove
    - POST /api/v2/time_entries
  description: The Time Entries Bulk endpoint enables efficient management of multiple time entries simultaneously.
time_entry_versions:
  file: time_entry_versions.yaml
  operations:
    - GET /api/v2/time_entry_versions
    - GET /api/v2/time_entry_versions/{id}
  description: Time entry versions store the history of changes on a time entry.
time_tracking_policies:
  file: time_tracking_policies.yaml
  operations:
    - DELETE /api/v2/time_tracking_policies/{id}
    - GET /api/v2/time_tracking_policies
    - GET /api/v2/time_tracking_policies/{id}
    - PATCH /api/v2/time_tracking_policies/{id}
    - PATCH /api/v2/time_tracking_policies/{id}/archive
    - PATCH /api/v2/time_tracking_policies/{id}/restore
    - POST /api/v2/time_tracking_policies
  description: '**Time tracking policies** define rules governing how and when time entries can be logged — restricting tracking to specific services, enforcing duration limits, or requiring prior approval.'
timers:
  file: timers.yaml
  operations:
    - GET /api/v2/timers
    - GET /api/v2/timers/{id}
    - PATCH /api/v2/timers/{id}/stop
    - POST /api/v2/timers
  description: Timer is an object belonging to a Time Entry.
timesheets:
  file: timesheets.yaml
  operations:
    - DELETE /api/v2/timesheets/{id}
    - GET /api/v2/timesheets
    - GET /api/v2/timesheets/{id}
    - POST /api/v2/timesheets
  description: '**Timesheets** aggregate a person''s time entries by day and are the unit of submission in the time approval workflow.'
todos:
  file: todos.yaml
  operations:
    - DELETE /api/v2/todos/{id}
    - GET /api/v2/todos
    - GET /api/v2/todos/{id}
    - PATCH /api/v2/todos/{id}
    - POST /api/v2/todos
  description: A to-do is a component of a task that requires completion and can be assigned to an individual.
users:
  file: users.yaml
  operations:
    - GET /api/v2/users
    - GET /api/v2/users/{id}
    - PATCH /api/v2/users/{id}
    - PATCH /api/v2/users/{id}/update_password
  description: '**User accounts** for people who log into Productive. Manage credentials, preferences, and authentication settings.'
webhook_logs:
  file: webhook_logs.yaml
  operations:
    - GET /api/v2/webhook_logs
  description: The Webhook Log object represents a record of an unsuccessful webhook delivery attempt. When a webhook fails to deliver, a log is created with details about the failure.
webhooks:
  file: webhooks.yaml
  operations:
    - DELETE /api/v2/webhooks/{id}
    - GET /api/v2/webhooks
    - GET /api/v2/webhooks/{id}
    - POST /api/v2/webhooks
  description: For more details on webhooks please refer to the [help article](https://help.productive.io/en/articles/3693279-using-webhooks-in-productive).
widgets:
  file: widgets.yaml
  operations:
    - DELETE /api/v2/widgets/{id}
    - GET /api/v2/widgets
    - GET /api/v2/widgets/{id}
    - PATCH /api/v2/widgets/{id}
    - POST /api/v2/widgets
  description: The Widget object represents the fundamental dynamic component of the Dashboard system.
workflow_statuses:
  file: workflow_statuses.yaml
  operations:
    - DELETE /api/v2/workflow_statuses/{id}
    - GET /api/v2/workflow_statuses
    - GET /api/v2/workflow_statuses/{id}
    - PATCH /api/v2/workflow_statuses/{id}
    - POST /api/v2/workflow_statuses
  description: The Workflow Status object represents the current state of a task, enabling users to define and monitor task stages.
workflows:
  file: workflows.yaml
  operations:
    - DELETE /api/v2/workflows/{id}
    - GET /api/v2/workflows
    - GET /api/v2/workflows/{id}
    - PATCH /api/v2/workflows/{id}
    - PATCH /api/v2/workflows/{id}/archive
    - PATCH /api/v2/workflows/{id}/restore
    - POST /api/v2/workflows
  description: The Workflow object enables organizing tasks into distinct phases or stages, each with its own set of statuses.
