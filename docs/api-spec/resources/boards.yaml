openapi: 3.1.0
info:
  title: Productive.io API – Boards
  version: 2.0.0
description: |-
  The Board object represents a board within the project management system. It sits between Projects and Task lists, meaning it belongs to a project and contains multiple task lists.

  In the application, `boards` are currently referred to as `folders`.

  You can find out more about boards in our Help documentation: [Boards](https://help.productive.io/en/articles/2179655-multiple-boards)

  Following diagram shows boards in our data hierarchy:

  ```d2
  ...@template
  # template cant import globs just yet
  * {
    class: default
  }

  # Define actors
  project: Project
  board: Board {
    class: primary
  }
  task_list: Task list
  task: Task
  comments: Comments
  subtasks: Subtasks
  todos: Todos

  # define connections
  project -> board -> task_list -> task
  task -> comments
  task -> subtasks
  task -> todos
  ```
x-filters:
  board:
    status:
      description: Filter by active/archived status.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    project_id:
      description: Filter by the associated project.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    query:
      description: Search boards by name (text search).
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    id:
      description: Filter by board ID.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
paths:
  /api/v2/boards/{id}/archive:
    patch:
      tags:
        - Boards
      summary: Archives a board
      responses:
        '200':
          $ref: '#/components/responses/single_board'
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID
        - $ref: '#/components/parameters/header_organization'
      operationId: boards-archive-archive
      description: Archive a board.
  /api/v2/boards/copy:
    post:
      tags:
        - Boards
      summary: Copy a board
      responses:
        '200':
          $ref: '#/components/responses/single_board'
        '201':
          content:
            application/vnd.api+json: {}
          headers:
            Content-Type:
              schema:
                type: string
          description: Created
      parameters:
        - $ref: '#/components/parameters/header_organization'
      operationId: boards-copy-copy
      description: Copy a board.
  /api/v2/boards:
    get:
      tags:
        - Boards
      summary: Get boards
      responses:
        '200':
          $ref: '#/components/responses/collection_board'
      parameters:
        - $ref: '#/components/parameters/sort_board'
        - $ref: '#/components/parameters/header_organization'
        - $ref: '#/components/parameters/filter_board'
      operationId: boards-index
      description: List boards with optional filters and pagination.
    post:
      tags:
        - Boards
      summary: Create a board
      responses:
        '201':
          $ref: '#/components/responses/single_board'
        '422':
          content:
            application/vnd.api+json: {}
          headers:
            Content-Type:
              schema:
                type: string
          description: Unprocessable Entity
      parameters:
        - $ref: '#/components/parameters/header_organization'
      operationId: boards-create
      requestBody:
        $ref: '#/components/requestBodies/board'
      description: Create a new board.
  /api/v2/boards/{id}/move:
    patch:
      tags:
        - Boards
      summary: Move a board
      responses:
        '200':
          $ref: '#/components/responses/single_board'
        '422':
          content:
            application/vnd.api+json: {}
          headers:
            Content-Type:
              schema:
                type: string
          description: Unprocessable Entity
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID
        - $ref: '#/components/parameters/header_organization'
      operationId: boards-move-move
      description: Move a board.
  /api/v2/boards/{id}/reposition:
    patch:
      tags:
        - Boards
      summary: Repoisition a board
      responses:
        '200':
          $ref: '#/components/responses/single_board'
      parameters:
        - $ref: '#/components/parameters/header_organization'
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID
      operationId: boards-reposition-reposition
      description: Reposition a board.
  /api/v2/boards/{id}/restore:
    patch:
      tags:
        - Boards
      summary: Restores a board
      responses:
        '200':
          $ref: '#/components/responses/single_board'
      parameters:
        - $ref: '#/components/parameters/header_organization'
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID
      operationId: boards-restore-restore
      description: Restore a board.
  /api/v2/boards/{id}:
    get:
      tags:
        - Boards
      summary: Gets a board
      responses:
        '200':
          $ref: '#/components/responses/single_board'
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID
        - $ref: '#/components/parameters/header_organization'
      operationId: boards-show
      description: Retrieve a single board by ID.
    patch:
      tags:
        - Boards
      summary: Update a board
      responses:
        '200':
          $ref: '#/components/responses/single_board'
        '422':
          content:
            application/vnd.api+json: {}
          headers:
            Content-Type:
              schema:
                type: string
          description: Unprocessable Entity
      parameters:
        - $ref: '#/components/parameters/header_organization'
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID
      operationId: boards-update
      requestBody:
        $ref: '#/components/requestBodies/board'
      description: Update a board.
components:
  requestBodies:
    board:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties:
                  attributes:
                    type: object
                    required:
                      - name
                    properties:
                      name:
                        $ref: '#/components/schemas/resource_board/properties/name'
                      hidden:
                        $ref: '#/components/schemas/resource_board/properties/hidden'
                      position:
                        $ref: '#/components/schemas/resource_board/properties/position'
                      project_id:
                        $ref: '#/components/schemas/resource_board/properties/project_id'
      description: Request body for creating or updating a board (folder). Specify the project, name, position, placement (top or bottom), and optional archived status or hidden flag.
  schemas:
    resource_board:
      type: object
      title: Board Resource
      properties:
        project:
          title: Project
          description: The project this folder belongs to.
          example:
            eq: value
        query:
          type: string
          title: Query
          description: A saved filter query string applied to this folder view.
          example:
            eq: value
        id:
          type: integer
          title: Id
          description: Unique identifier for the folder.
          example:
            - 123
        name:
          type: string
          title: Name
          description: The display name of the folder.
          example:
            eq: value
        placement:
          type: integer
          title: Placement
          description: Indicates the folder's placement context (e.g. project or global).
          example:
            eq: value
        status:
          enum:
            - 1
            - 2
          type: integer
          title: Status
          description: The current status of the folder (e.g. active or archived).
          example:
            eq: active
        position:
          type: integer
          title: Position
          description: The display order position of this folder within its project.
          example:
            eq: value
        organization:
          title: Organization
          description: The organization this folder belongs to.
          example:
            eq: value
        project_id:
          type: integer
          title: Project
          description: The ID of the project this folder belongs to.
          example:
            - 123
        hidden:
          type: boolean
          title: Hidden
          description: When true, this folder is hidden from members who do not have explicit access.
          example:
            eq: value
        archived_at:
          type: string
          title: Archived At
          format: date-time
          description: Timestamp when the folder was archived, or null if active.
          example:
            gt: '2026-01-01'
      description: Folder (called Board internally) grouping task lists within a project. Supports name, position ordering, placement (top or bottom), archiving, and hidden visibility. Used to organize task lists into phases or sprints.
      example:
        id: '5522'
        type: boards
        attributes:
          name: Features
          position: 3
          placement: 1030000
          archived_at: null
          hidden: false
        relationships:
          organization:
            data:
              type: organizations
              id: '109'
          project:
            meta:
              included: false
    _meta:
      type: object
      title: Meta
      properties:
        total_count:
          type: integer
          description: Total number of records matching the current query across all pages.
          example: 42
        organization_features:
          type: object
          description: Feature flags and capabilities enabled for the current organization, used for UI rendering.
          example: []
        total_pages:
          type: integer
          description: Total number of pages available for the current query and page size.
          example: 5
        current_page:
          type: integer
          description: The current page number in this paginated response (1-based).
          example: 1
        settings:
          type: object
          description: Organization or user settings relevant to this resource's display.
          example:
            feature_x: true
            notifications:
              email: true
        page_size:
          type: integer
          description: Number of records returned per page in this response.
          example: 30
        max_page_size:
          type: integer
          description: Maximum number of records allowed per page for this endpoint.
          example: 200
      description: Pagination and response metadata returned with every collection endpoint.
      example:
        current_page: 1
        total_pages: 5
        total_count: 42
        page_size: 30
        max_page_size: 200
    _single_relationship:
      anyOf:
        - type: object
          title: Single data
          properties:
            data:
              $ref: '#/components/schemas/_resource'
          description: Included variant — the related resource identifier is present in `data`.
          example:
            data:
              type: tasks
              id: '42'
        - $ref: '#/components/schemas/_not_included'
      description: A JSON:API single relationship — links to one related resource or `null`.
      example:
        data:
          type: tasks
          id: '42'
    _not_included:
      type: object
      title: Not included
      properties:
        meta:
          type: object
          properties:
            included:
              enum:
                - false
              type: boolean
              description: Whether the related resource was included in the sideloaded data. Always `false` for this variant.
              example: false
          description: Metadata indicating the sideload status of this relationship.
          example:
            included: false
      description: Placeholder for a relationship that was not sideloaded in this response.
      example:
        meta:
          included: false
    _resource:
      type: object
      title: Resource Identifier
      properties:
        id:
          type: integer
          description: The unique identifier of the resource.
          example: 42
        type:
          type: string
          description: The resource type name (e.g. `tasks`, `people`, `deals`).
          example: tasks
      description: A JSON:API resource identifier object containing the resource type and ID.
      example:
        type: tasks
        id: '42'
  parameters:
    header_organization:
      in: header
      name: X-Organization-Id
      schema:
        type: string
      required: true
      description: Organization ID
    filter_board:
      in: query
      name: filter
      style: deepObject
      schema:
        type: object
        description: Filter keys and their operators are listed under x-filters.board at the top of this file.
      required: false
      description: Filters
    sort_board:
      in: query
      name: sort
      style: form
      schema:
        type: array
        items:
          type: string
        title: Sort board
      explode: false
      required: false
      description: Sort board
  responses:
    single_board:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties:
                  attributes:
                    type: object
                    properties:
                      name:
                        $ref: '#/components/schemas/resource_board/properties/name'
                      hidden:
                        $ref: '#/components/schemas/resource_board/properties/hidden'
                      position:
                        $ref: '#/components/schemas/resource_board/properties/position'
                      placement:
                        $ref: '#/components/schemas/resource_board/properties/placement'
                      archived_at:
                        $ref: '#/components/schemas/resource_board/properties/archived_at'
                  relationships:
                    type: object
                    properties:
                      project:
                        $ref: '#/components/schemas/_single_relationship'
                      organization:
                        $ref: '#/components/schemas/_single_relationship'
              meta:
                $ref: '#/components/schemas/_meta'
              included:
                type: array
                items: {}
      description: Single board
    collection_board:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  type: object
                  properties:
                    attributes:
                      type: object
                      properties:
                        name:
                          $ref: '#/components/schemas/resource_board/properties/name'
                        hidden:
                          $ref: '#/components/schemas/resource_board/properties/hidden'
                        position:
                          $ref: '#/components/schemas/resource_board/properties/position'
                        placement:
                          $ref: '#/components/schemas/resource_board/properties/placement'
                        archived_at:
                          $ref: '#/components/schemas/resource_board/properties/archived_at'
                    relationships:
                      type: object
                      properties:
                        project:
                          $ref: '#/components/schemas/_single_relationship'
                        organization:
                          $ref: '#/components/schemas/_single_relationship'
              meta:
                $ref: '#/components/schemas/_meta'
              included:
                type: array
                items: {}
      description: Collection board
