openapi: 3.1.0
info:
  title: Productive.io API – Reports – entitlement_reports
  version: 2.0.0
x-filters:
  entitlement_report:
    end_date:
      description: Filter entitlement report results by end date.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    event_id:
      description: Filter entitlement report results by event id.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    used:
      description: Filter entitlement report results by used.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    person_id:
      description: Filter entitlement report results by person id.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    formulas:
      description: Filter entitlement report results by formulas.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    start_date:
      description: Filter entitlement report results by start date.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    allocated:
      description: Filter entitlement report results by allocated.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    id:
      description: Filter entitlement report results by id.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    date:
      description: Filter entitlement report results by date.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    absence_type:
      description: Filter entitlement report results by absence type.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
paths:
  /api/v2/reports/entitlement_reports:
    get:
      tags:
        - Reports
      summary: Get entitlement reports
      responses:
        '200':
          $ref: '#/components/responses/collection_entitlement_report'
      parameters:
        - $ref: '#/components/parameters/group_entitlement_report'
        - $ref: '#/components/parameters/sort_entitlement_report'
        - $ref: '#/components/parameters/header_organization'
        - $ref: '#/components/parameters/filter_entitlement_report'
      operationId: reports-entitlement_reports-index
      description: Retrieve aggregated entitlement report data grouped by configurable dimensions.
components:
  parameters:
    filter_entitlement_report:
      in: query
      name: filter
      style: deepObject
      schema:
        type: object
        description: Filter keys and their operators are listed under x-filters.entitlement_report at the top of this file.
      required: false
      description: Filters
    header_organization:
      in: header
      name: X-Organization-Id
      schema:
        type: string
      required: true
      description: Organization ID
    sort_entitlement_report:
      in: query
      name: sort
      style: form
      schema:
        type: array
        items:
          enum:
            - absence_type
            - -absence_type
            - count
            - -count
            - end_date
            - -end_date
            - entitlement
            - -entitlement
            - entitlement_end_date
            - -entitlement_end_date
            - entitlement_start_date
            - -entitlement_start_date
            - event
            - -event
            - people_custom_fields
            - -people_custom_fields
            - person
            - -person
            - start_date
            - -start_date
            - subsidiary
            - -subsidiary
            - total_allocated
            - -total_allocated
            - total_allocated_minutes
            - -total_allocated_minutes
            - total_available
            - -total_available
            - total_available_minutes
            - -total_available_minutes
            - total_pending
            - -total_pending
            - total_pending_minutes
            - -total_pending_minutes
            - total_used
            - -total_used
            - total_used_minutes
            - -total_used_minutes
          type: string
        title: Sort entitlement report
      explode: false
      required: false
      description: Sort entitlement report
    group_entitlement_report:
      in: query
      name: group
      style: form
      schema:
        enum:
          - absence_type
          - end_date
          - entitlement
          - event
          - organization
          - people_custom_fields
          - person
          - start_date
          - subsidiary
        type: string
        title: Group entitlement report
      explode: false
      required: false
      description: Group entitlement report
  schemas:
    resource_entitlement_report:
      type: object
      title: Entitlement report Resource
      properties:
        used:
          type: number
          title: Used
          description: Hours used in this entitlement row's absence category.
          example:
            eq: value
        start_date_period:
          title: Start Date Period
          description: The start date period descriptor used for grouping in time-based reports.
          example:
            gt: '2026-01-01'
        entitlement_end_date:
          title: Entitlement End Date
          description: The end date of the entitlement record linked to this report row.
          example:
            gt: '2026-01-01'
        absence_type:
          enum:
            - time_off
            - remote_work
          type: string
          title: Absence Type
          description: The type of absence (e.g. time off, remote work) for this entitlement report row.
          example:
            eq: active
        group:
          type: string
          title: Group
          description: The grouping key for this report row when results are aggregated by a dimension.
          example:
            eq: value
        entitlement_start_date:
          title: Entitlement Start Date
          description: The start date of the entitlement record linked to this report row.
          example:
            gt: '2026-01-01'
        event:
          title: Event
          description: The absence category (event) this report row covers.
          example:
            eq: value
        entitlement:
          title: Entitlement
          description: The linked entitlement record this report row is based on.
          example:
            eq: value
        count:
          type: integer
          title: Count
          description: Number of entitlement records included in this aggregated report row.
          example:
            gt: 0
        allocated:
          type: number
          title: Allocated
          description: Total hours allocated for the absence category in the reporting period.
          example:
            eq: value
        formulas:
          type: integer
          title: Formulas
          description: Saved formula definitions applied to generate calculated columns in this report.
          example:
            eq: value
        event_id:
          type: integer
          title: Event
          description: The ID of the absence category (event) for this report row.
          example:
            - 123
        currency:
          title: Currency
          description: The currency code of financial values in this report row.
          example:
            eq: value
        total_available:
          type: number
          title: Total Available
          description: Total available hours remaining (allocated minus used and pending).
          example:
            eq: value
        person_id:
          type: integer
          title: Person
          description: The ID of the person this entitlement report row is for.
          example:
            - 123
        total_allocated:
          type: number
          title: Total Allocated
          description: Total allocated hours for this person across all entitlement records in the period.
          example:
            eq: value
        total_allocated_minutes:
          type: integer
          title: Total Allocated Minutes
          description: Total allocated minutes for this person in the entitlement period (raw minutes value).
          example:
            eq: value
        total_pending:
          type: number
          title: Total Pending
          description: Total pending time-off or entitlement time across grouped entries, in the configured time unit.
          example:
            eq: value
        formula_fields:
          title: Formula Fields
          description: Custom formula field values computed for this report row.
          example:
            eq: value
        id:
          type: integer
          title: Id
          description: The unique identifier of this entitlement report row.
          example:
            - 123
        currency_default:
          title: Currency Default
          description: Financial values expressed in the organization's default currency.
          example:
            eq: value
        total_available_minutes:
          type: integer
          title: Total Available Minutes
          description: Total available minutes remaining (raw minutes value).
          example:
            eq: value
        end_date_period:
          title: End Date Period
          description: The end date period descriptor used for grouping in time-based reports.
          example:
            gt: '2026-01-01'
        total_pending_minutes:
          title: Total Pending Minutes
          description: Total pending time-off or entitlement time across grouped entries, expressed in minutes.
          example:
            eq: value
        date:
          type: string
          title: Date
          format: date
          description: The reporting date for this entitlement row.
          example:
            gt: '2026-01-01'
        total_used_minutes:
          type: integer
          title: Total Used Minutes
          description: Total used minutes (raw minutes value).
          example:
            eq: value
        people_custom_fields:
          title: People Custom Fields
          description: Custom field values from the associated person record included for reporting context.
          example:
            eq: value
        report:
          title: Report
          description: The parent report record this row belongs to.
          example:
            eq: value
        total_used:
          type: number
          title: Total Used
          description: Total used hours — approved absences taken within the entitlement period.
          example:
            eq: value
        end_date:
          type: string
          title: End Date
          format: date
          description: The end date of the entitlement period covered by this report row.
          example:
            gt: '2026-01-01'
        person_report:
          title: Person Report
          description: Summary person-level report data rolled up from individual entitlement records.
          example:
            eq: value
        start_date:
          type: string
          title: Start Date
          format: date
          description: The start date of the entitlement period covered by this report row.
          example:
            gt: '2026-01-01'
        currency_normalized:
          title: Currency Normalized
          description: Financial values normalized for cross-currency comparison.
          example:
            eq: value
        subsidiary:
          title: Subsidiary
          description: The subsidiary associated with this entitlement report row.
          example:
            eq: value
        organization:
          title: Organization
          description: The organization this entitlement report row belongs to.
          example:
            eq: value
        person:
          title: Person
          description: The person this entitlement report row is for.
          example:
            eq: value
      description: An aggregated entitlement report row combining a person's time-off allocation, usage, and availability for an absence category over a period.
      example:
        id: '123'
        type: entitlement_reports
        attributes:
          feature: time_tracking
          usage_count: 42
          organization_id: 1
        relationships: {}
    _meta:
      type: object
      title: Meta
      properties:
        total_count:
          type: integer
          description: Total number of records matching the current query across all pages.
          example: 42
        organization_features:
          type: object
          description: Feature flags and capabilities enabled for the current organization, used for UI rendering.
          example: []
        total_pages:
          type: integer
          description: Total number of pages available for the current query and page size.
          example: 5
        current_page:
          type: integer
          description: The current page number in this paginated response (1-based).
          example: 1
        settings:
          type: object
          description: Organization or user settings relevant to this resource's display.
          example:
            feature_x: true
            notifications:
              email: true
        page_size:
          type: integer
          description: Number of records returned per page in this response.
          example: 30
        max_page_size:
          type: integer
          description: Maximum number of records allowed per page for this endpoint.
          example: 200
      description: Pagination and response metadata returned with every collection endpoint.
      example:
        current_page: 1
        total_pages: 5
        total_count: 42
        page_size: 30
        max_page_size: 200
    _single_relationship:
      anyOf:
        - type: object
          title: Single data
          properties:
            data:
              $ref: '#/components/schemas/_resource'
          description: Included variant — the related resource identifier is present in `data`.
          example:
            data:
              type: tasks
              id: '42'
        - $ref: '#/components/schemas/_not_included'
      description: A JSON:API single relationship — links to one related resource or `null`.
      example:
        data:
          type: tasks
          id: '42'
    _not_included:
      type: object
      title: Not included
      properties:
        meta:
          type: object
          properties:
            included:
              enum:
                - false
              type: boolean
              description: Whether the related resource was included in the sideloaded data. Always `false` for this variant.
              example: false
          description: Metadata indicating the sideload status of this relationship.
          example:
            included: false
      description: Placeholder for a relationship that was not sideloaded in this response.
      example:
        meta:
          included: false
    _resource:
      type: object
      title: Resource Identifier
      properties:
        id:
          type: integer
          description: The unique identifier of the resource.
          example: 42
        type:
          type: string
          description: The resource type name (e.g. `tasks`, `people`, `deals`).
          example: tasks
      description: A JSON:API resource identifier object containing the resource type and ID.
      example:
        type: tasks
        id: '42'
  responses:
    collection_entitlement_report:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  type: object
                  properties:
                    attributes:
                      type: object
                      properties:
                        count:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/count'
                        group:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/group'
                        currency:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/currency'
                        total_used:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/total_used'
                        absence_type:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/absence_type'
                        total_pending:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/total_pending'
                        formula_fields:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/formula_fields'
                        end_date_period:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/end_date_period'
                        total_allocated:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/total_allocated'
                        total_available:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/total_available'
                        currency_default:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/currency_default'
                        start_date_period:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/start_date_period'
                        total_used_minutes:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/total_used_minutes'
                        currency_normalized:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/currency_normalized'
                        people_custom_fields:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/people_custom_fields'
                        total_pending_minutes:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/total_pending_minutes'
                        total_allocated_minutes:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/total_allocated_minutes'
                        total_available_minutes:
                          $ref: '#/components/schemas/resource_entitlement_report/properties/total_available_minutes'
                    relationships:
                      type: object
                      properties:
                        event:
                          $ref: '#/components/schemas/_single_relationship'
                        person:
                          $ref: '#/components/schemas/_single_relationship'
                        report:
                          $ref: '#/components/schemas/_single_relationship'
                        subsidiary:
                          $ref: '#/components/schemas/_single_relationship'
                        entitlement:
                          $ref: '#/components/schemas/_single_relationship'
                        organization:
                          $ref: '#/components/schemas/_single_relationship'
                        person_report:
                          $ref: '#/components/schemas/_single_relationship'
              meta:
                $ref: '#/components/schemas/_meta'
              included:
                type: array
                items: {}
      description: Collection entitlement report
