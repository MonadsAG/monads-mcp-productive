openapi: 3.1.0
info:
  title: Productive.io API – Reports – salary_reports
  version: 2.0.0
x-filters:
  new_salary_report:
    annuall_cost:
      description: Filter new salary report results by annuall cost.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    people_custom_fields:
      description: Filter new salary report results by people custom fields.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    ended_on:
      description: Filter new salary report results by ended on.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    overhead:
      description: Filter new salary report results by overhead.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    formulas:
      description: Filter new salary report results by formulas.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    monthly_cost:
      description: Filter new salary report results by monthly cost.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    estimated_weekly_hours:
      description: Filter new salary report results by estimated weekly hours.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    hourly_cost:
      description: Filter new salary report results by hourly cost.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    salary_id:
      description: Filter new salary report results by salary id.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    time:
      description: Filter new salary report results by time.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    started_on:
      description: Filter new salary report results by started on.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    holiday_calendar_id:
      description: Filter new salary report results by holiday calendar id.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    salary_type_id:
      description: Filter new salary report results by salary type id.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    person_id:
      description: Filter new salary report results by person id.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    id:
      description: Filter new salary report results by id.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    weekly_cost:
      description: Filter new salary report results by weekly cost.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    date:
      description: Filter new salary report results by date.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    bi_weekly_cost:
      description: Filter new salary report results by bi weekly cost.
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
paths:
  /api/v2/reports/salary_reports:
    get:
      tags:
        - Reports
      summary: Get salary reports
      responses:
        '200':
          $ref: '#/components/responses/collection_new_salary_report'
      parameters:
        - $ref: '#/components/parameters/header_organization'
        - $ref: '#/components/parameters/filter_new_salary_report'
        - $ref: '#/components/parameters/group_new_salary_report'
        - $ref: '#/components/parameters/sort_new_salary_report'
      operationId: reports-salary_reports-index
      description: Retrieve aggregated salary report data grouped by configurable dimensions.
components:
  parameters:
    sort_new_salary_report:
      in: query
      name: sort
      style: form
      schema:
        type: array
        items:
          enum:
            - count
            - -count
            - date
            - -date
            - engagement_ended_on
            - -engagement_ended_on
            - engagement_started_on
            - -engagement_started_on
            - estimated_weekly_hours
            - -estimated_weekly_hours
            - overhead
            - -overhead
            - people_custom_fields
            - -people_custom_fields
            - person
            - -person
            - salary_id
            - -salary_id
            - salary_type_id
            - -salary_type_id
            - time
            - -time
            - total_annually_cost
            - -total_annually_cost
            - total_bi_weekly_cost
            - -total_bi_weekly_cost
            - total_hourly_cost
            - -total_hourly_cost
            - total_monthly_cost
            - -total_monthly_cost
            - total_weekly_cost
            - -total_weekly_cost
          type: string
        title: Sort new salary report
      explode: false
      required: false
      description: Sort new salary report
    group_new_salary_report:
      in: query
      name: group
      style: form
      schema:
        enum:
          - date
          - organization
          - overhead
          - people_custom_fields
          - person
          - salary
          - salary_type_id
        type: string
        title: Group new salary report
      explode: false
      required: false
      description: Group new salary report
    filter_new_salary_report:
      in: query
      name: filter
      style: deepObject
      schema:
        type: object
        description: Filter keys and their operators are listed under x-filters.new_salary_report at the top of this file.
      required: false
      description: Filters
    header_organization:
      in: header
      name: X-Organization-Id
      schema:
        type: string
      required: true
      description: Organization ID
  schemas:
    resource_new_salary_report:
      type: object
      title: New salary report Resource
      properties:
        total_period_cost:
          title: Total Period Cost
          description: Sum of period cost across all records in this new salary report group.
          example:
            eq: value
        total_bi_weekly_cost_normalized:
          title: Total Bi Weekly Cost Normalized
          description: Sum of bi weekly cost in the normalized currency across all records in this new salary report group.
          example:
            eq: value
        total_overhead_cost_per_hour_default:
          title: Total Overhead Cost Per Hour Default
          description: Sum of overhead cost per hour in the organization's default currency across all records in this new salary report group.
          example:
            eq: value
        report:
          title: Report
          description: Report for this new salary report group.
          example:
            eq: value
        engagement_ended_on:
          title: Engagement Ended On
          description: Engagement ended on for this new salary report group.
          example:
            eq: value
        salary_id:
          type: integer
          title: Salary
          description: Salary id for this new salary report group.
          example:
            - 123
        currency_default:
          title: Currency Default
          description: Currency in the organization's default currency for this new salary report group.
          example:
            eq: value
        total_hourly_cost_normalized:
          title: Total Hourly Cost Normalized
          description: Sum of hourly cost in the normalized currency across all records in this new salary report group.
          example:
            eq: value
        hourly_cost:
          title: Hourly Cost
          description: Hourly cost for this new salary report group.
          example:
            eq: value
        salary:
          title: Salary
          description: Salary for this new salary report group.
          example:
            eq: value
        id:
          type: integer
          title: Id
          description: Unique identifier for this new salary report row.
          example:
            - 123
        annuall_cost:
          title: Annuall Cost
          description: Annuall cost for this new salary report group.
          example:
            eq: value
        total_monthly_cost_default:
          title: Total Monthly Cost Default
          description: Sum of monthly cost in the organization's default currency across all records in this new salary report group.
          example:
            eq: value
        total_monthly_cost_normalized:
          title: Total Monthly Cost Normalized
          description: Sum of monthly cost in the normalized currency across all records in this new salary report group.
          example:
            eq: value
        person:
          title: Person
          description: Person for this new salary report group.
          example:
            eq: value
        engagement_started_on:
          title: Engagement Started On
          description: Engagement started on for this new salary report group.
          example:
            eq: value
        total_period_cost_default:
          title: Total Period Cost Default
          description: Sum of period cost in the organization's default currency across all records in this new salary report group.
          example:
            eq: value
        capacity:
          type: number
          title: Capacity
          description: Capacity for this new salary report group.
          example:
            eq: value
        count:
          type: integer
          title: Count
          description: Number of new salarys in this report group.
          example:
            gt: 0
        salary_type_id:
          type: integer
          title: Salary Type
          description: Salary type id for this new salary report group.
          example:
            - 123
        total_annually_cost:
          title: Total Annually Cost
          description: Sum of annually cost across all records in this new salary report group.
          example:
            eq: value
        total_bi_weekly_cost:
          title: Total Bi Weekly Cost
          description: Sum of bi weekly cost across all records in this new salary report group.
          example:
            eq: value
        total_bi_weekly_cost_default:
          title: Total Bi Weekly Cost Default
          description: Sum of bi weekly cost in the organization's default currency across all records in this new salary report group.
          example:
            eq: value
        person_report:
          title: Person Report
          description: The person associated with this salary report row.
          example:
            eq: value
        formula_fields:
          title: Formula Fields
          description: Formula fields for this new salary report group.
          example:
            eq: value
        total_weekly_cost_normalized:
          title: Total Weekly Cost Normalized
          description: Sum of weekly cost in the normalized currency across all records in this new salary report group.
          example:
            eq: value
        currency:
          type: string
          title: Currency
          description: Currency for this new salary report group.
          example:
            eq: value
        total_estimated_weekly_hours:
          type: number
          title: Total Estimated Weekly Hours
          description: Sum of estimated weekly hours across all records in this new salary report group.
          example:
            gt: 0
        total_overhead_cost_per_hour:
          title: Total Overhead Cost Per Hour
          description: Sum of overhead cost per hour across all records in this new salary report group.
          example:
            eq: value
        monthly_cost:
          title: Monthly Cost
          description: Monthly cost for this new salary report group.
          example:
            eq: value
        estimated_weekly_hours:
          type: integer
          title: Estimated Weekly Hours
          description: Estimated weekly hours for this new salary report group.
          example:
            gt: 0
        engagement:
          title: Engagement
          description: Engagement for this new salary report group.
          example:
            eq: value
        started_on:
          type: string
          title: Started On
          format: date
          description: Started on for this new salary report group.
          example:
            eq: value
        people_custom_fields:
          type: string
          title: People Custom Fields
          description: People custom fields for this new salary report group.
          example:
            eq: value
        date:
          type: string
          title: Date
          format: date
          description: Date for this new salary report group.
          example:
            gt: '2026-01-01'
        end_date_period:
          title: End Date Period
          description: Time period grouping for end date (e.g. week, month, quarter).
          example:
            gt: '2026-01-01'
        bi_weekly_cost:
          title: Bi Weekly Cost
          description: Bi weekly cost for this new salary report group.
          example:
            eq: value
        start_date_period:
          title: Start Date Period
          description: Time period grouping for start date (e.g. week, month, quarter).
          example:
            gt: '2026-01-01'
        weekly_cost:
          title: Weekly Cost
          description: Weekly cost for this new salary report group.
          example:
            eq: value
        total_annually_cost_default:
          title: Total Annually Cost Default
          description: Sum of annually cost in the organization's default currency across all records in this new salary report group.
          example:
            eq: value
        holiday_calendar_id:
          type: integer
          title: Holiday Calendar
          description: Holiday calendar id for this new salary report group.
          example:
            - 123
        total_hourly_cost:
          title: Total Hourly Cost
          description: Sum of hourly cost across all records in this new salary report group.
          example:
            eq: value
        total_weekly_cost:
          title: Total Weekly Cost
          description: Sum of weekly cost across all records in this new salary report group.
          example:
            eq: value
        time:
          type: number
          title: Time
          description: Time for this new salary report group.
          example:
            gt: 0
        formulas:
          type: integer
          title: Formulas
          description: Formulas for this new salary report group.
          example:
            eq: value
        total_monthly_cost:
          title: Total Monthly Cost
          description: Sum of monthly cost across all records in this new salary report group.
          example:
            eq: value
        work_days:
          type: number
          title: Work Days
          description: Work days for this new salary report group.
          example:
            eq: value
        total_annually_cost_normalized:
          title: Total Annually Cost Normalized
          description: Sum of annually cost in the normalized currency across all records in this new salary report group.
          example:
            eq: value
        person_id:
          type: integer
          title: Person
          description: Person id for this new salary report group.
          example:
            - 123
        total_period_cost_normalized:
          title: Total Period Cost Normalized
          description: Sum of period cost in the normalized currency across all records in this new salary report group.
          example:
            eq: value
        group:
          type: string
          title: Group
          description: The grouping dimension for this new salary report row.
          example:
            eq: value
        total_overhead_cost_per_hour_normalized:
          title: Total Overhead Cost Per Hour Normalized
          description: Sum of overhead cost per hour in the normalized currency across all records in this new salary report group.
          example:
            eq: value
        total_weekly_cost_default:
          title: Total Weekly Cost Default
          description: Sum of weekly cost in the organization's default currency across all records in this new salary report group.
          example:
            eq: value
        overhead:
          type: integer
          title: Overhead
          description: Overhead for this new salary report group.
          example:
            eq: value
        date_period:
          title: Date Period
          description: Time period grouping for date (e.g. week, month, quarter).
          example:
            eq: value
        organization:
          title: Organization
          description: Organization for this new salary report group.
          example:
            eq: value
        total_hourly_cost_default:
          title: Total Hourly Cost Default
          description: Sum of hourly cost in the organization's default currency across all records in this new salary report group.
          example:
            eq: value
        currency_normalized:
          title: Currency Normalized
          description: Currency in the normalized currency for this new salary report group.
          example:
            eq: value
        ended_on:
          type: string
          title: Ended On
          format: date
          description: Ended on for this new salary report group.
          example:
            eq: value
      description: An aggregated salary report row grouping people by configurable dimensions with salary cost metrics.
      example:
        id: '123'
        type: new_salary_reports
        attributes:
          total_salary: 12000
          month: 2026-01
          person_id: 12
        relationships: {}
    _meta:
      type: object
      title: Meta
      properties:
        total_count:
          type: integer
          description: Total number of records matching the current query across all pages.
          example: 42
        organization_features:
          type: object
          description: Feature flags and capabilities enabled for the current organization, used for UI rendering.
          example: []
        total_pages:
          type: integer
          description: Total number of pages available for the current query and page size.
          example: 5
        current_page:
          type: integer
          description: The current page number in this paginated response (1-based).
          example: 1
        settings:
          type: object
          description: Organization or user settings relevant to this resource's display.
          example:
            feature_x: true
            notifications:
              email: true
        page_size:
          type: integer
          description: Number of records returned per page in this response.
          example: 30
        max_page_size:
          type: integer
          description: Maximum number of records allowed per page for this endpoint.
          example: 200
      description: Pagination and response metadata returned with every collection endpoint.
      example:
        current_page: 1
        total_pages: 5
        total_count: 42
        page_size: 30
        max_page_size: 200
    _single_relationship:
      anyOf:
        - type: object
          title: Single data
          properties:
            data:
              $ref: '#/components/schemas/_resource'
          description: Included variant — the related resource identifier is present in `data`.
          example:
            data:
              type: tasks
              id: '42'
        - $ref: '#/components/schemas/_not_included'
      description: A JSON:API single relationship — links to one related resource or `null`.
      example:
        data:
          type: tasks
          id: '42'
    _not_included:
      type: object
      title: Not included
      properties:
        meta:
          type: object
          properties:
            included:
              enum:
                - false
              type: boolean
              description: Whether the related resource was included in the sideloaded data. Always `false` for this variant.
              example: false
          description: Metadata indicating the sideload status of this relationship.
          example:
            included: false
      description: Placeholder for a relationship that was not sideloaded in this response.
      example:
        meta:
          included: false
    _resource:
      type: object
      title: Resource Identifier
      properties:
        id:
          type: integer
          description: The unique identifier of the resource.
          example: 42
        type:
          type: string
          description: The resource type name (e.g. `tasks`, `people`, `deals`).
          example: tasks
      description: A JSON:API resource identifier object containing the resource type and ID.
      example:
        type: tasks
        id: '42'
  responses:
    collection_new_salary_report:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  type: object
                  properties:
                    attributes:
                      type: object
                      properties:
                        time:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/time'
                        count:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/count'
                        group:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/group'
                        capacity:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/capacity'
                        currency:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/currency'
                        overhead:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/overhead'
                        work_days:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/work_days'
                        date_period:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/date_period'
                        formula_fields:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/formula_fields'
                        salary_type_id:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/salary_type_id'
                        end_date_period:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/end_date_period'
                        currency_default:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/currency_default'
                        start_date_period:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/start_date_period'
                        total_hourly_cost:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_hourly_cost'
                        total_period_cost:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_period_cost'
                        total_weekly_cost:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_weekly_cost'
                        total_monthly_cost:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_monthly_cost'
                        currency_normalized:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/currency_normalized'
                        total_annually_cost:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_annually_cost'
                        people_custom_fields:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/people_custom_fields'
                        total_bi_weekly_cost:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_bi_weekly_cost'
                        total_hourly_cost_default:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_hourly_cost_default'
                        total_period_cost_default:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_period_cost_default'
                        total_weekly_cost_default:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_weekly_cost_default'
                        total_monthly_cost_default:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_monthly_cost_default'
                        total_annually_cost_default:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_annually_cost_default'
                        total_bi_weekly_cost_default:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_bi_weekly_cost_default'
                        total_estimated_weekly_hours:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_estimated_weekly_hours'
                        total_hourly_cost_normalized:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_hourly_cost_normalized'
                        total_overhead_cost_per_hour:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_overhead_cost_per_hour'
                        total_period_cost_normalized:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_period_cost_normalized'
                        total_weekly_cost_normalized:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_weekly_cost_normalized'
                        total_monthly_cost_normalized:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_monthly_cost_normalized'
                        total_annually_cost_normalized:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_annually_cost_normalized'
                        total_bi_weekly_cost_normalized:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_bi_weekly_cost_normalized'
                        total_overhead_cost_per_hour_default:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_overhead_cost_per_hour_default'
                        total_overhead_cost_per_hour_normalized:
                          $ref: '#/components/schemas/resource_new_salary_report/properties/total_overhead_cost_per_hour_normalized'
                    relationships:
                      type: object
                      properties:
                        person:
                          $ref: '#/components/schemas/_single_relationship'
                        report:
                          $ref: '#/components/schemas/_single_relationship'
                        salary:
                          $ref: '#/components/schemas/_single_relationship'
                        organization:
                          $ref: '#/components/schemas/_single_relationship'
                        person_report:
                          $ref: '#/components/schemas/_single_relationship'
              meta:
                $ref: '#/components/schemas/_meta'
              included:
                type: array
                items: {}
      description: Collection new salary report
