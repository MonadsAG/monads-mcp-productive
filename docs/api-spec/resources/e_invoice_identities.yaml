openapi: 3.1.0
info:
  title: Productive.io API – E-invoice Identities
  version: 2.0.0
x-filters:
  einvoice_identity:
    company_id:
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
    subsidiary_id:
      operators:
        - contains
        - eq
        - not_contain
        - not_eq
paths:
  /api/v2/einvoice_identities:
    get:
      tags:
        - E-invoice Identities
      summary: Gets a list of e-invoice identities
      responses:
        '200':
          $ref: '#/components/responses/collection_einvoice_identity'
      parameters:
        - $ref: '#/components/parameters/header_organization'
        - $ref: '#/components/parameters/filter_einvoice_identity'
      operationId: einvoice_identities-index
    post:
      tags:
        - E-invoice Identities
      summary: Creates an e-invoice identity
      responses:
        '201':
          $ref: '#/components/responses/single_einvoice_identity'
        '422':
          content:
            application/vnd.api+json: {}
          headers:
            Content-Type:
              schema:
                type: string
          description: Unprocessable Entity
      parameters:
        - $ref: '#/components/parameters/header_organization'
      operationId: einvoice_identities-create
      requestBody:
        $ref: '#/components/requestBodies/einvoice_identity'
  /api/v2/einvoice_identities/{id}:
    get:
      tags:
        - E-invoice Identities
      summary: Gets an e-invoice identity
      responses:
        '200':
          $ref: '#/components/responses/single_einvoice_identity'
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
        - $ref: '#/components/parameters/header_organization'
      operationId: einvoice_identities-show
    patch:
      tags:
        - E-invoice Identities
      summary: Updates an e-invoice identity
      responses:
        '200':
          $ref: '#/components/responses/single_einvoice_identity'
        '422':
          content:
            application/vnd.api+json: {}
          headers:
            Content-Type:
              schema:
                type: string
          description: Unprocessable Entity
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
        - $ref: '#/components/parameters/header_organization'
      operationId: einvoice_identities-update
      requestBody:
        $ref: '#/components/requestBodies/einvoice_identity'
components:
  requestBodies:
    einvoice_identity:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties:
                  attributes:
                    type: object
                    properties:
                      dire_code:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/dire_code'
                      peppol_id:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/peppol_id'
                      company_id:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/company_id'
                      subsidiary_id:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/subsidiary_id'
                      buyer_reference:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/buyer_reference'
                      dir3_fiscal_code:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/dir3_fiscal_code'
                      dir3_pagador_code:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/dir3_pagador_code'
                      dir3_receptor_code:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/dir3_receptor_code'
                      is_government_entity:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/is_government_entity'
  schemas:
    resource_einvoice_identity:
      type: object
      title: E-invoice Identity Resource
      properties:
        subsidiary_id:
          type: integer
          title: Subsidiary
          example: '42'
        dir3_fiscal_code:
          type: string
          title: Dir3 Fiscal Code
          example: A01234567
        peppol_id:
          type: string
          title: Peppol Id
          example: 0088:1234567890123
        dire_code:
          type: string
          title: Dire Code
          example: DIR3-EXAMPLE
        updated_at:
          type: string
          title: Updated At
          format: date-time
          example: '2025-06-01T14:22:00.000Z'
        is_government_entity:
          type: boolean
          title: Is Government Entity
          example: false
        created_at:
          type: string
          title: Created At
          format: date-time
          example: '2025-03-15T10:30:00.000Z'
        subsidiary:
          title: Subsidiary
          example:
            data:
              type: subsidiaries
              id: '42'
        dir3_receptor_code:
          type: string
          title: Dir3 Receptor Code
          example: L01234567
        buyer_reference:
          type: string
          title: Buyer Reference
          example: BUYER-REF-001
        company:
          title: Company
          example:
            data:
              type: companies
              id: '89'
        company_id:
          type: integer
          title: Company
          example: '89'
        dir3_pagador_code:
          type: string
          title: Dir3 Pagador Code
          example: P00000010
      example:
        id: '567'
        type: einvoice_identities
        attributes:
          peppol_id: 0088:1234567890123
          buyer_reference: BUYER-REF-001
          dir3_fiscal_code: null
          dir3_pagador_code: null
          dir3_receptor_code: null
          dire_code: null
          is_government_entity: false
          created_at: '2025-04-10T08:15:00.000Z'
          updated_at: '2025-04-10T08:15:00.000Z'
        relationships:
          company:
            data:
              type: companies
              id: '89'
    _meta:
      type: object
      title: Meta
      properties:
        current_page:
          type: integer
          description: The current page number in this paginated response (1-based).
          example: 1
        total_pages:
          type: integer
          description: Total number of pages available for the current query and page size.
          example: 5
        settings:
          type: object
          description: Organization or user settings relevant to this resource's display.
          example:
            feature_x: true
            notifications:
              email: true
        total_count:
          type: integer
          description: Total number of records matching the current query across all pages.
          example: 42
        page_size:
          type: integer
          description: Number of records returned per page in this response.
          example: 30
        max_page_size:
          type: integer
          description: Maximum number of records allowed per page for this endpoint.
          example: 200
        organization_features:
          type: object
          description: Feature flags and capabilities enabled for the current organization, used for UI rendering.
          example: []
      description: Pagination and response metadata returned with every collection endpoint.
      example:
        current_page: 1
        total_pages: 5
        total_count: 42
        page_size: 30
        max_page_size: 200
    _single_relationship:
      anyOf:
        - type: object
          title: Single data
          properties:
            data:
              $ref: '#/components/schemas/_resource'
          description: Included variant — the related resource identifier is present in `data`.
          example:
            data:
              type: tasks
              id: '42'
        - $ref: '#/components/schemas/_not_included'
      description: A JSON:API single relationship — links to one related resource or `null`.
      example:
        data:
          type: tasks
          id: '42'
    _not_included:
      type: object
      title: Not included
      properties:
        meta:
          type: object
          properties:
            included:
              enum:
                - false
              type: boolean
              description: Whether the related resource was included in the sideloaded data. Always `false` for this variant.
              example: false
          description: Metadata indicating the sideload status of this relationship.
          example:
            included: false
      description: Placeholder for a relationship that was not sideloaded in this response.
      example:
        meta:
          included: false
    _resource:
      type: object
      title: Resource Identifier
      properties:
        type:
          type: string
          description: The resource type name (e.g. `tasks`, `people`, `deals`).
          example: tasks
        id:
          type: integer
          description: The unique identifier of the resource.
          example: 42
      description: A JSON:API resource identifier object containing the resource type and ID.
      example:
        type: tasks
        id: '42'
  parameters:
    header_organization:
      in: header
      name: X-Organization-Id
      schema:
        type: string
      required: true
      description: Organization ID
    filter_einvoice_identity:
      in: query
      name: filter
      style: deepObject
      schema:
        type: object
        description: Filter keys and their operators are listed under x-filters.einvoice_identity at the top of this file.
      required: false
  responses:
    single_einvoice_identity:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties:
                  attributes:
                    type: object
                    properties:
                      dire_code:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/dire_code'
                      peppol_id:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/peppol_id'
                      created_at:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/created_at'
                      updated_at:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/updated_at'
                      buyer_reference:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/buyer_reference'
                      dir3_fiscal_code:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/dir3_fiscal_code'
                      dir3_pagador_code:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/dir3_pagador_code'
                      dir3_receptor_code:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/dir3_receptor_code'
                      is_government_entity:
                        $ref: '#/components/schemas/resource_einvoice_identity/properties/is_government_entity'
                  relationships:
                    type: object
                    properties:
                      company:
                        $ref: '#/components/schemas/_single_relationship'
                      subsidiary:
                        $ref: '#/components/schemas/_single_relationship'
              meta:
                $ref: '#/components/schemas/_meta'
              included:
                type: array
                items: {}
      description: Single einvoice identity
    collection_einvoice_identity:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  type: object
                  properties:
                    attributes:
                      type: object
                      properties:
                        dire_code:
                          $ref: '#/components/schemas/resource_einvoice_identity/properties/dire_code'
                        peppol_id:
                          $ref: '#/components/schemas/resource_einvoice_identity/properties/peppol_id'
                        created_at:
                          $ref: '#/components/schemas/resource_einvoice_identity/properties/created_at'
                        updated_at:
                          $ref: '#/components/schemas/resource_einvoice_identity/properties/updated_at'
                        buyer_reference:
                          $ref: '#/components/schemas/resource_einvoice_identity/properties/buyer_reference'
                        dir3_fiscal_code:
                          $ref: '#/components/schemas/resource_einvoice_identity/properties/dir3_fiscal_code'
                        dir3_pagador_code:
                          $ref: '#/components/schemas/resource_einvoice_identity/properties/dir3_pagador_code'
                        dir3_receptor_code:
                          $ref: '#/components/schemas/resource_einvoice_identity/properties/dir3_receptor_code'
                        is_government_entity:
                          $ref: '#/components/schemas/resource_einvoice_identity/properties/is_government_entity'
                    relationships:
                      type: object
                      properties:
                        company:
                          $ref: '#/components/schemas/_single_relationship'
                        subsidiary:
                          $ref: '#/components/schemas/_single_relationship'
              meta:
                $ref: '#/components/schemas/_meta'
              included:
                type: array
                items: {}
      description: Collection of einvoice identities
