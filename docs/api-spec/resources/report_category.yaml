openapi: 3.1.0
info:
  title: Productive.io API – Report Category
  version: 2.0.0
description: |
  Report categories help you organize and categorize your reports. You can assign each category a unique name and color code, making it easier to navigate through and manage your saved reports. This visual organization allows you to quickly find the reports you need and keep your workspace more streamlined.
paths:
  /api/v2/report_categories:
    get:
      tags:
        - Report Category
      summary: Get report categories
      responses:
        '200':
          $ref: '#/components/responses/collection_report_category'
      parameters:
        - $ref: '#/components/parameters/header_organization'
        - $ref: '#/components/parameters/sort_report_category'
        - $ref: '#/components/parameters/filter_report_category'
      operationId: report_categories-index
      description: List all report categories.
    post:
      tags:
        - Report Category
      summary: Create a report category
      responses:
        '201':
          $ref: '#/components/responses/single_report_category'
        '422':
          content:
            application/vnd.api+json: {}
          headers:
            Content-Type:
              schema:
                type: string
          description: Unprocessable Entity
      parameters:
        - $ref: '#/components/parameters/header_organization'
      operationId: report_categories-create
      requestBody:
        $ref: '#/components/requestBodies/report_category'
      description: Create a new report category.
  /api/v2/report_categories/{id}:
    get:
      tags:
        - Report Category
      summary: Gets a report category
      responses:
        '200':
          $ref: '#/components/responses/single_report_category'
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID
        - $ref: '#/components/parameters/header_organization'
      operationId: report_categories-show
      description: Retrieve a specific report category by ID.
    patch:
      tags:
        - Report Category
      summary: Update a report category
      responses:
        '200':
          $ref: '#/components/responses/single_report_category'
        '422':
          content:
            application/vnd.api+json: {}
          headers:
            Content-Type:
              schema:
                type: string
          description: Unprocessable Entity
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID
        - $ref: '#/components/parameters/header_organization'
      operationId: report_categories-update
      requestBody:
        $ref: '#/components/requestBodies/report_category'
      description: Update an existing report category.
    delete:
      tags:
        - Report Category
      summary: Deletes a report category
      responses:
        '204':
          description: No Content
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID
        - $ref: '#/components/parameters/header_organization'
      operationId: report_categories-destroy
      description: Delete a report category.
components:
  parameters:
    header_organization:
      in: header
      name: X-Organization-Id
      schema:
        type: string
      required: true
      description: Organization ID
    filter_report_category:
      in: query
      name: filter
      style: deepObject
      schema:
        type: object
        description: Filter keys and their operators are listed under x-filters.report_category at the top of this file.
      required: false
      description: Filters
    sort_report_category:
      in: query
      name: sort
      style: form
      schema:
        type: array
        items:
          type: string
        title: Sort report category
      explode: false
      required: false
      description: Sort report category
  requestBodies:
    report_category:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties:
                  attributes:
                    type: object
                    required:
                      - name
                    properties:
                      name:
                        $ref: '#/components/schemas/resource_report_category/properties/name'
                      color_id:
                        $ref: '#/components/schemas/resource_report_category/properties/color_id'
                      position:
                        $ref: '#/components/schemas/resource_report_category/properties/position'
      description: Request body for creating or updating a report category.
  schemas:
    resource_report_category:
      type: object
      title: Report category Resource
      properties:
        color_id:
          type: integer
          title: Color
          description: Color identifier for this report category.
          example:
            - 123
        name:
          type: string
          title: Name
          description: Display name of this report category.
          example:
            eq: value
        organization:
          title: Organization
          description: The organization this report category belongs to.
          example:
            eq: value
        deleted_at:
          type: string
          title: Deleted At
          format: date-time
          description: Timestamp when this report category was deleted.
          example:
            gt: '2026-01-01'
        position:
          type: integer
          title: Position
          description: Display order position of this report category.
          example:
            eq: value
      description: An aggregated report category resource grouping saved report filters.
      example:
        id: '123'
        type: report_categories
        attributes:
          name: Financial Reports
          description: Revenue and cost reports
        relationships: {}
    _meta:
      type: object
      title: Meta
      properties:
        total_count:
          type: integer
          description: Total number of records matching the current query across all pages.
          example: 42
        organization_features:
          type: object
          description: Feature flags and capabilities enabled for the current organization, used for UI rendering.
          example: []
        total_pages:
          type: integer
          description: Total number of pages available for the current query and page size.
          example: 5
        current_page:
          type: integer
          description: The current page number in this paginated response (1-based).
          example: 1
        settings:
          type: object
          description: Organization or user settings relevant to this resource's display.
          example:
            feature_x: true
            notifications:
              email: true
        page_size:
          type: integer
          description: Number of records returned per page in this response.
          example: 30
        max_page_size:
          type: integer
          description: Maximum number of records allowed per page for this endpoint.
          example: 200
      description: Pagination and response metadata returned with every collection endpoint.
      example:
        current_page: 1
        total_pages: 5
        total_count: 42
        page_size: 30
        max_page_size: 200
    _single_relationship:
      anyOf:
        - type: object
          title: Single data
          properties:
            data:
              $ref: '#/components/schemas/_resource'
          description: Included variant — the related resource identifier is present in `data`.
          example:
            data:
              type: tasks
              id: '42'
        - $ref: '#/components/schemas/_not_included'
      description: A JSON:API single relationship — links to one related resource or `null`.
      example:
        data:
          type: tasks
          id: '42'
    _not_included:
      type: object
      title: Not included
      properties:
        meta:
          type: object
          properties:
            included:
              enum:
                - false
              type: boolean
              description: Whether the related resource was included in the sideloaded data. Always `false` for this variant.
              example: false
          description: Metadata indicating the sideload status of this relationship.
          example:
            included: false
      description: Placeholder for a relationship that was not sideloaded in this response.
      example:
        meta:
          included: false
    _resource:
      type: object
      title: Resource Identifier
      properties:
        id:
          type: integer
          description: The unique identifier of the resource.
          example: 42
        type:
          type: string
          description: The resource type name (e.g. `tasks`, `people`, `deals`).
          example: tasks
      description: A JSON:API resource identifier object containing the resource type and ID.
      example:
        type: tasks
        id: '42'
  responses:
    single_report_category:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties:
                  attributes:
                    type: object
                    properties:
                      name:
                        $ref: '#/components/schemas/resource_report_category/properties/name'
                      color_id:
                        $ref: '#/components/schemas/resource_report_category/properties/color_id'
                      position:
                        $ref: '#/components/schemas/resource_report_category/properties/position'
                      deleted_at:
                        $ref: '#/components/schemas/resource_report_category/properties/deleted_at'
                  relationships:
                    type: object
                    properties:
                      organization:
                        $ref: '#/components/schemas/_single_relationship'
              meta:
                $ref: '#/components/schemas/_meta'
              included:
                type: array
                items: {}
      description: Single report category
    collection_report_category:
      content:
        application/vnd.api+json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  type: object
                  properties:
                    attributes:
                      type: object
                      properties:
                        name:
                          $ref: '#/components/schemas/resource_report_category/properties/name'
                        color_id:
                          $ref: '#/components/schemas/resource_report_category/properties/color_id'
                        position:
                          $ref: '#/components/schemas/resource_report_category/properties/position'
                        deleted_at:
                          $ref: '#/components/schemas/resource_report_category/properties/deleted_at'
                    relationships:
                      type: object
                      properties:
                        organization:
                          $ref: '#/components/schemas/_single_relationship'
              meta:
                $ref: '#/components/schemas/_meta'
              included:
                type: array
                items: {}
      description: Collection report category
